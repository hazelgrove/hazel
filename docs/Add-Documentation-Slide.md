The initial state for Scratch and Documentation is generated in `src/web/init`.

To download the current buffer, click `Nut Menu` > `File` > `Export for Init`, or open the command palette `cmd+k`, and select `Export for Init`.

Add the resulting file to `web/init/docs`, and add the entry to `web/init/Init.re`.
