fun model, action ->
    case action
    | InputChanged(password) => { model with password }
    | StrengthChanged(strength) => { model with strength } 
    | Everything(All as all) => { model with All }
    end
