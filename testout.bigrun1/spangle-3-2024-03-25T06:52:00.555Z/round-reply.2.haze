fun (model, action) ->
  case action
  | ChangePassword(password) => {model with password = password; strength = calculateStrength(password, model.criteria)}
  | ChangeStrength(strength) => {model with strength = strength_of(strength)}
  | ChangeCriteria(criteria) => {model with criteria = criteria}
  end