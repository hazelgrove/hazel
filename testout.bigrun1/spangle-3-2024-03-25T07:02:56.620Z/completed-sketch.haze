# Update Password Strength Checker app model based on an action #

let update: (Model, Action) -> Model =
  
fun model, action ->
  case action
  | ChangePassword(password) => { model | password = password, strength = check_strength(password) }
  | _ => model
  end
in