fun model, action ->
    case action
    | CreateRoom(title) => 
        {
            model with
            rooms = model.rooms @ [title];
        }
    | BookRoom(index) => 
        {
            model with
            rooms = List.mapi (fun i room -> if i == index then "Booked " ++ room else room) model.rooms;
        }