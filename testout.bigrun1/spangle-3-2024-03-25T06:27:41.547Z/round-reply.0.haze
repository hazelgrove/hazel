fun model, action ->
  case action
  | ClearGrid => model_init 
  | ChangeEmoji(emoji) => model(emoji)
  | FillRow(row) => { model with grid = fillRowInGrid(model.grid, row, model.emoji) }
  | PlaceEmoji(row, col) => { model with grid = updateGrid(model.grid, row, col, model.emoji) }
  end