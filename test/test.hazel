let intsOneElseAdd: ((Int, Int)) -> Int = 
    fun (x, y) -> 
        case (x, y) 
            | (1, 1) => 1
            | _ => x + y
        end
in

let ap_and_inc: (((Int, Int) -> Int), Int) -> Int = 
    fun (f, x) -> f((x, 2)) + 1
in

ap_and_inc((intsOneElseAdd, 1))
