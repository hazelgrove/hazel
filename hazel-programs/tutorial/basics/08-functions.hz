@flags
show_report version=7 id=a0000006-0006-0006-0006-000000000006

@prompt
Functions are expressions that take inputs and produce outputs. In Hazel, you write an anonymous function as:
```hazelnostatics
fun x -> body
```
where `x` is the parameter and `body` is an expression that can use `x`.

# Task
The editor below contains a function triple with the function body missing. The function is then applied to 10.

Complete the function so that the result evaluates to 30.

@hint
Complete the function body and apply it

@reference
### Function Definition
```hazel
fun n -> n + 1 # Increments n by 1 #
```

### Binding and Calling a Function
```hazel
let inc : Int -> Int = 
fun n -> n + 1
in
inc(1)
```

@hints
triple(10) should be 30
triple(0) should be 0
triple(1) should be 3

@code
let triple : Int -> Int = fun n -> ¿ in
triple(10)

@test
test triple(10) == 30 end;
test triple(0) == 0 end;
test triple(1) == 3 end
