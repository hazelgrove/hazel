type GradebookEntry = (
  name= String,
  age= Int,
  quiz1= Int,
  quiz2= Int,
  midterm= Int,
  quiz3= Int,
  quiz4= Int,
  final= Int
) in
let gradebook : [GradebookEntry] = (
  [
    ("Bob",   12, 8, 9, 77, 7, 9, 87),
    ("Alice", 17, 6, 8, 88, 8, 7, 85),
    ("Eve", 13, 7, 9, 84, 8, 8, 77)
  ]
) in

# (benchmark) distinct #
let requires = [] in
let ensures = [
  (enforced=("static"), "schema(t2) is equal to schema(t1)")
] in
let distinct =
  typfun row ->
    fun (t : [row]) ->
      fold_left(
        t,
        fun (
          acc,
          elem
        ) -> if mem(acc, elem) then acc else acc @ [elem],
        []
      )
      : [row] in
test
  distinct@<(quiz3= Int)>(map(gradebook, select_labels(_, `quiz3`)))
  == [7, 8] : [(quiz3= Int)] end
