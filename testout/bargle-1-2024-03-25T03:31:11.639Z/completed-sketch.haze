# Update Playlist app model based on an action #

let update: (PlayList, PlayListAction) -> PlayList =
  
fun model, action ->
  case action
  | Add(song) => model ++ [song]
  | Remove(index) => remove_index(index, model)
  | ChangeName(new_name) => update_name(new_name, model)
  end
in