fun model, action ->
  case action
  | SetPointerLocation(x, y) => {model | pointerLocation = (x, y)}
  | SetDrawMode(drawMode) => {model | drawMode = drawMode}
  | AddEmoji(emoji) => {model | canvas = Canvas.add_emoji(model.canvas, model.pointerLocation, emoji)}
  end