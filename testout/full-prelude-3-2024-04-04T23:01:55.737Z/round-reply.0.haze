fun model, action ->
    case action
    | AddTodo => (fst(model), add(model))
    | RemoveTodo(index) => (fst(model), remove(index, snd(model)))
    | ToggleTodo(index) => (fst(model), toggle(index, snd(model)))
    | UpdateBuffer(str) => (str, snd(model))
    end