# Update Password Strength Checker app model based on an action #

let update: (Model, Action) -> Model =
  fun (model, action) ->
  case action
  | ChangePassword(password) => { password = password }
  | ValidatePassword => { validation = validate(model.password) }
  | Reset => initialModel
  end
in