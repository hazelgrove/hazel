# Update playlist based on action #
let update: (PlayList, PlayListAction) -> PlayList =
  fun (playlist, action) ->
    case action
    | Add(song) => song::playlist
    | Remove(artist, title) => List.filter(fun (a, t) -> (a, t) <> (artist, title), playlist)
    | Clear => [] 
  end
in