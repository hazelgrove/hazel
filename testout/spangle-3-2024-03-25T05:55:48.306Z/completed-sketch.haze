# Update Playlist app model based on an action #

let update: (PlayList, PlayListAction) -> PlayList =
  fun (model, action) ->
  case action
  | PlayListAction.Add(song) => song::model
  | PlayListAction.Remove(song) => List.filter(fun s -> s != song, model)
  | PlayListAction.Clear => [] 
  end
in