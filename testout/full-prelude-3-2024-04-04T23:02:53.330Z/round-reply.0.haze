
fun (model, action) ->
  case action
  | AddTodo => (Model.init, add(model))
  | RemoveTodo(index) => (Model.init, remove(index, model))
  | ToggleTodo(index) => (Model.init, toggle(index, model))
  | UpdateBuffer(str) => (str, model)
  end
