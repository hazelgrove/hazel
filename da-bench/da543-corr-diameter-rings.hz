# DA-Bench id 543: corr Diameter vs Rings + status, abalone.csv #
let data = ^^csv("abalone.csv") in
let r = round2(corr_cols(data.`Diameter`, data.`Rings`)) in
(correlation_coefficient = r, relationship_status = if abs_float(r) >=. 0.1 then "correlate" else "none")
