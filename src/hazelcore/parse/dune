(include_subdirs no)

(library
 (name parser)
 (libraries pretty hazelcore menhirLib)
 (preprocess
  (pps ppx_let)))

(ocamllex
 (modules hazel_lexer))

(menhir
 (flags --table)
 (modules hazel_parser))
