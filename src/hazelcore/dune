(include_subdirs unqualified)

(library
 (name hazelcore)
 (libraries core_kernel.weak_hashtbl re sexplib)
 (wrapped false)
 (preprocess
  (pps ppx_sexp_conv)))

(ocamllex
 (modules SkelExprLexer SkelPatLexer SkelTypLexer))

(menhir
 (modules SkelExprParser SkelPatParser SkelTypParser))
