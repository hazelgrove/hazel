(include_subdirs unqualified)

(library
 (name pretty)
 (instrumentation
  (backend bisect_ppx)))

(env
 (dev
  (js_of_ocaml
   (flags :standard --debuginfo --noinline --dynlink --linkall --sourcemap)))
 (release
  (js_of_ocaml
   (flags :standard))))
