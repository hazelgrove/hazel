open Util;

let init = (_schedule_action: Page.Update.t => unit): unit => {
  ();
    /* TODO: Implement proper postMessage bridge for external projectors
     * This will handle communication between iframe-embedded external apps
     * and the Hazel editor for SetSyntax actions */
};
