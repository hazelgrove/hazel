(* Auto-generated by: ./hazel gen-slides -- DO NOT EDIT *)
(* Source: hazel-programs/study/writing/crop-tally/crop-tally-solution.hz *)

let out : string * Haz3lcore.PersistentZipper.t =
  ( "Study / writing / crop-tally / crop-tally-solution",
    Haz3lcore.PersistentZipper.of_slide_text
      {x|# Crop Tally - Solution #

let garden = [
"fern orchid fern cactus",
"orchid starfern fern orchid",
"cactus fern orchid orchid"
] in

let count_in_row: (String, String) -> Int =
fun (row, plant) ->
let words = string_split(" ", row) in
let matches = filter(words, fun w -> w == plant) in
length(matches)
in

let count_in_garden: ([String], String) -> Int =
fun (garden, plant) ->
let counts = map(garden, fun row -> count_in_row(row, plant)) in
fold_left(counts, fun (acc, n) -> acc + n, 0)
in

test count_in_row("fern orchid fern cactus", "fern") == 2 end;

test count_in_row("orchid starfern fern orchid", "fern") == 1 end;

test count_in_garden(garden, "fern") == 4 end;

test count_in_garden(garden, "orchid") == 5 end;

test count_in_garden(garden, "cactus") == 2 end
|x}
  )
