(* Auto-generated by: ./hazel gen-tutorial -- DO NOT EDIT *)
(* Source: hazel-programs/tutorial/basics/18-labeled-tuples.hz *)

let exercise : Tutorial.spec = {
  id = Option.get (Haz3lcore.Id.of_string "32aa8e06-b0d4-4459-8879-13575a45768b");
  title = "Basics / 18 - Labeled Tuples";
  version = 1;
  module_name = "TuGen_Basics18LabeledTuples";
  prompt = {x|Hazel supports labeled tuples, which let you associate labels with tuple elements — giving an ordered version of records. They also support projection by label:

```hazel
let pet = (name="Fido", age=4, species="dog") in
pet.name
```

# Task

Complete the labeled tuple below so that the x-coordinate equals the y-coordinate.|x};
  display_hint = {x|Pick any x and y as long as they're the same|x};
  task_reference = {x|### Labeled Tuple Projection
Access a field of a labeled tuple with `.`:
```hazel
let pet =
(name="Fido", 
age=4, 
species="dog") in
pet.name
```|x};
  your_impl =
    Haz3lcore.Zipper.caret_to_start (Option.get (Haz3lcore.TextRoundtrip.of_text ~root:Exp {x|type Point = (x=Float, y=Float) in

let p : Point = ¿ in
p|x}));
  hidden_tests =
    {
      tests =
        Option.get (Haz3lcore.TextRoundtrip.of_text ~root:Exp {x|test p.x == p.y end|x});
      hints = ["Make sure they're the same and both floats"];
    };
  wrapper = false;
  show_report = true;
}
