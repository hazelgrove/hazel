(* Auto-generated by: ./hazel gen-tutorial -- DO NOT EDIT *)
(* Source: hazel-programs/tutorial/conjoined/15-fold.hz *)

let exercise : Tutorial.spec =
  {
    id =
      Option.get (Haz3lcore.Id.of_string "b0000013-0013-0013-0013-000000000013");
    title = "15 - Fold";
    version = 1;
    module_name = "TuGen_15Fold";
    prompt =
      {x|`fold_left` combines a list into a single value, carrying an accumulator from left to right:
```hazelnostatics
fold_left(list, fun (acc, x) -> ... , start)
```

# Task
Total the garden's water with `fold_left`, starting from `0`.|x};
    display_hint = {x|Add each element `w` to the running accumulator `acc`.|x};
    task_reference =
      {x|### Fold
```hazel
fold_left([1, 2, 3], fun (acc, x) -> acc + x, 0)
```
`acc` carries the running result; `x` is the current element.|x};
    your_impl =
      Haz3lcore.Zipper.caret_to_start
        (Option.get
           (Haz3lcore.TextRoundtrip.of_text ~root:Exp
              {x|let waters = [250, 180, 50, 200] in
fold_left(
waters,
fun (acc, w) ->
¿
, 0)|x}));
    hidden_tests =
      {
        tests =
          Option.get
            (Haz3lcore.TextRoundtrip.of_text ~root:Exp
               {x|test answer == 680 end|x});
        hints = [ "The total is 680." ];
      };
    wrapper = true;
    show_report = true;
  }
