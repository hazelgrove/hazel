(* Auto-generated by: ./hazel gen-tutorial -- DO NOT EDIT *)
(* Source: hazel-programs/tutorial/conjoined/12-auto-probe.hz *)

let exercise : Tutorial.spec = {
  id = Option.get (Haz3lcore.Id.of_string "b0000010-0010-0010-0010-000000000010");
  title = "12 - Auto Probe";
  version = 1;
  module_name = "TuGen_12AutoProbe";
  prompt = {x|So far you have placed probes by hand. **Auto-probe** does it for you: turn it on and Hazel puts a multi probe on whichever definition your cursor is in, following you as you move.

Toggle it with **Cmd/Ctrl+P**, or open the probarium sidebar (the **🔬** icon on the right) and use the top toggle.

**Try this**
1. Turn on auto-probe, then click inside the `watering_amount` function. A probe appears on each line.
2. Move your cursor into `watering_amount`. The probes follow you.
3. Similarly, move your cursor to the body of the program (last line); again, the probes should follow
4. Return to ``watering_amount`. Note how since the parameters are on one line, you get a combined value for both at the end. Insert some linebreaks to put the parameters on different lines. Notice how formatting code in this way lets you decide how granular the intermediate values shown are.|x};
  display_hint = {x|Cmd/Ctrl+P, then move your cursor between the two functions.|x};
  task_reference = {x|### Probes
- **Auto-probe**: Cmd/Ctrl+P. Probes the definition your cursor is in, and follows the cursor.
- More line breaks show more intermediate values.|x};
  your_impl =
    Haz3lcore.Zipper.caret_to_start (Option.get (Haz3lcore.TextRoundtrip.of_text ~root:Exp {x|type MoonPhase = New + Waxing + Full + Waning in
let watering_amount(base: Int, phase: MoonPhase): Int =
case phase
| New => base + 50
| Full => base - 30
| _ => base
end
in
let weekly_water(base: Int, phase: MoonPhase): Int =
watering_amount(base, phase) * 7
in
weekly_water(250, Full)|x}));
  hidden_tests =
    {
      tests =
        Option.get (Haz3lcore.TextRoundtrip.of_text ~root:Exp {x|test true end|x});
      hints = [];
    };
  wrapper = false;
  show_report = false;
}
