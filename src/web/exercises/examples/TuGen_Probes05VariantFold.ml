(* Auto-generated by: ./hazel gen-tutorial -- DO NOT EDIT *)
(* Source: hazel-programs/tutorial/probes/05-variant-fold.hz *)

let exercise : Tutorial.spec = {
  id = Option.get (Haz3lcore.Id.of_string "7000001e-7507-4000-8000-000000000000");
  title = "Probes / 05 - Variant Fold";
  version = 1;
  module_name = "TuGen_Probes05VariantFold";
  prompt = {x|Work through the inline instructions in the editor below.|x};
  display_hint = {x||x};
  task_reference = {x||x};
  your_impl =
    Haz3lcore.Move.to_start (Option.get (Haz3lcore.TextRoundtrip.of_text ~root:Exp {x|# PART 5 VARIANT: STEP INTO + FOLD #

# fold_left processes a list one element at a time, #
# building up a result. From the outside, you just see #
# a number. Step Into lets you enter the function and #
# watch the accumulator grow step by step. #

# ============================================================ #

type Plant = (
name = String,
icon = String,
water = Int
) in

let fern: Plant = (name = "Fern", icon = "🌿", water = 250) in
let orchid: Plant = (name = "Orchid", icon = "🌸", water = 180) in
let cactus: Plant = (name = "Cactus", icon = "🍄", water = 50) in
let lily: Plant = (name = "Lily", icon = "☘️", water = 200) in
let daisy: Plant = (name = "Daisy", icon = "🌱", water = 160) in

let total_water: [Plant] -> Int =
fun plants ->
let add_water = fun (acc, plant) ->
let added = plant.water in
acc + added
in
fold_left(plants, add_water, 0)
in

# Three calls producing three numbers. But how did #
# those numbers get computed? #

# 1. Add a probe to each of the 3 expressions below. #
#    You see: 610, 230, 840. Just numbers. #
# 2. Click the sample for `total_water(all)` and press #
#    Enter (or select "Step Into" from the dropdown). #
# 3. Your cursor jumps inside `total_water`! Turn on #
#    auto-probe to see the fold callback's values. #
# 4. Switch to Many mode. You can see the accumulator #
#    building: `added` shows each plant's water, and #
#    `acc + added` shows the running total growing: #
#    250 → 430 → 480 → 680 → 840. #
# 5. Use the dynamic cursor bar at the top to navigate #
#    back to the call site. #

let shade = [fern, lily, daisy] in
let sun = [orchid, cactus] in
let all = [fern, orchid, cactus, lily, daisy] in

total_water(shade);
total_water(sun);
total_water(all)

# END #|x}));
  hidden_tests =
    {
      tests =
        Option.get (Haz3lcore.TextRoundtrip.of_text ~root:Exp {x|test true end|x});
      hints = [];
    };
  wrapper = false;
  show_report = false;
}
