(* Auto-generated by: ./hazel gen-tutorial -- DO NOT EDIT *)
(* Source: hazel-programs/tutorial/basics/15-mapping-over-lists.hz *)

let exercise : Tutorial.spec = {
  id = Option.get (Haz3lcore.Id.of_string "a0000014-0014-0014-0014-000000000014");
  title = "Basics / 15 - Mapping Over Lists";
  version = 15;
  module_name = "TuGen_Basics15MappingOverLists";
  prompt = {x|The built-in function `map` applies a function to every element of a list and returns a new list of the results. It has the signature `map : ([T], T -> U) -> [U]`.

For example:
```hazel
map([1, 2, 3], fun x -> x + 1)
```

Implement a function `double_all` of type `[Int] -> [Int]` that doubles every element of a list using `map`.|x};
  display_hint = {x|Use map with a function that doubles each element|x};
  task_reference = {x|### map
```hazelnostatics
map : ([T], T -> U) -> [U]
```

---

Apply a function to each element:
```hazel
map([1, 2, 3], fun x -> x + 1)
```


### Integer Arithmetic
- `2 + 3` — addition
- `5 - 1` — subtraction
- `4 * 3` — multiplication
- `10 / 3` — integer division|x};
  your_impl =
    Haz3lcore.Move.to_start (Option.get (Haz3lcore.TextRoundtrip.of_text ~root:Exp {x|let double_all : [Int] -> [Int] = ¿ in
double_all([1, 2, 3])|x}));
  hidden_tests =
    {
      tests =
        Option.get (Haz3lcore.TextRoundtrip.of_text ~root:Exp {x|test double_all([1, 2, 3]) == [2, 4, 6] end;
test double_all([]) == [] end;
test double_all([5]) == [10] end|x});
      hints = ["double_all([1, 2, 3]) should be [2, 4, 6]"; "double_all([]) should be []"; "double_all([5]) should be [10]"];
    };
  wrapper = false;
  show_report = true;
}
