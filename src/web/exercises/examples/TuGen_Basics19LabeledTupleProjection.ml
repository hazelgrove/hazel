(* Auto-generated by: ./hazel gen-tutorial -- DO NOT EDIT *)
(* Source: hazel-programs/tutorial/basics/19-labeled-tuple-projection.hz *)

let exercise : Tutorial.spec = {
  id = Option.get (Haz3lcore.Id.of_string "a89174ca-034d-43d7-b478-330388a2d712");
  title = "Basics / 19 - Labeled Tuple Projection";
  version = 1;
  module_name = "TuGen_Basics19LabeledTupleProjection";
  prompt = {x|Hazel's labeled tuples support record-style projection:

```hazel
let pet = (name="Fido", age=4, species="dog") in
pet.name
```

# Task

Implement a function:
```hazelnostatics
greeting : Person -> String
```

The greeting should be of the form `Hello {first_name} {last_name}!`. Use `++` to concatenate strings.

**Example:**
```hazelnostatics
greeting((first_name="Jane", last_name="Smith", age=20))
== "Hello Jane Smith!"
```|x};
  display_hint = {x|Define a function and use projection with string concatenation to build the string|x};
  task_reference = {x|### Function Definition
```hazel
fun n -> n + 1 # Increments n by 1 #
```

### Binding and Calling a Function
```hazel
let inc : Int -> Int =
fun n -> n + 1
in
inc(1)
```

### Labeled Tuple Projection
Access a field of a labeled tuple with `.`:
```hazel
let pet =
(name="Fido", 
age=4, 
species="dog") in
pet.name
```


### String Concatenation
```hazel
"hello" ++ " world"
```|x};
  your_impl =
    Haz3lcore.Zipper.caret_to_start (Option.get (Haz3lcore.TextRoundtrip.of_text ~root:Exp {x|type Person = (first_name=String, last_name=String, age=Int) in
let greeting : (Person -> String) = ¿ in
greeting((first_name="George", last_name="Garcia", age=12))|x}));
  hidden_tests =
    {
      tests =
        Option.get (Haz3lcore.TextRoundtrip.of_text ~root:Exp {x|test greeting((first_name="Jane", last_name="Smith",age=20)) == "Hello Jane Smith!" end;
test greeting((first_name="John", last_name="Baker", age=25)) == "Hello John Baker!" end|x});
      hints = ["Don't forget spaces"; "Don't forget the exclamation point!"];
    };
  wrapper = false;
  show_report = true;
}
