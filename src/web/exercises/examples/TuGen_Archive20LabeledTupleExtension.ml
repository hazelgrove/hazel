(* Auto-generated by: ./hazel gen-tutorial -- DO NOT EDIT *)
(* Source: hazel-programs/tutorial/archive/20-labeled-tuple-extension.hz *)

let exercise : Tutorial.spec = {
  id = Option.get (Haz3lcore.Id.of_string "fd42ef8a-11ce-4e7b-9993-e50f2bb096e7");
  title = "Archive / 20 - Labeled Tuple Extension";
  version = 1;
  module_name = "TuGen_Archive20LabeledTupleExtension";
  prompt = {x|To easily update and extend labeled tuples, Hazel provides an extension operator (`...`).

This binary operator takes two labeled tuples and merges them: it updates the left-hand tuple using the fields from the right-hand tuple. Existing fields are overwritten with the new values, and entirely new fields are appended to the end of the tuple.

**Example:**

```hazel
let pet = (name="Spot", age=7, species="Dog") in
pet ... (age=8, breed="Pug")
```

# Task

Implement a function `update_person` that takes a person tuple and:

- increases the `age` field by 1.
- adds a new `name` field that combines the first and last names (e.g. `"Thor Odinson"`).

```hazelnostatics
update_person((first="Thor", age=30, last="Odinson"))
==
(first="Thor", age=31, last="Odinson", name="Thor Odinson")
```|x};
  display_hint = {x|Use tuple extension `(...)` to update the `age` field and the `name` field|x};
  task_reference = {x|### Tuple Extension
Use `...` to update or add fields to a labeled tuple:
```hazel
let pet = (name="Spot", age=7) in
pet ... (age=8, breed="Pug")
```


### Labeled Tuple Projection
Access a field of a labeled tuple with `.`:
```hazel
let pet =
(name="Fido", 
age=4, 
species="dog") in
pet.name
```


### String Concatenation
```hazel
"hello" ++ " world"
```

### Integer Arithmetic
- `2 + 3` — addition
- `5 - 1` — subtraction
- `4 * 3` — multiplication
- `10 / 3` — integer division|x};
  your_impl =
    Haz3lcore.Zipper.caret_to_start (Option.get (Haz3lcore.TextRoundtrip.of_text ~root:Exp {x|type Person = (first=String, age=Int, last=String) in
let update_person = fun (p : Person) ->
¿
in
update_person((first="Thor", age=30, last="Odinson"))|x}));
  hidden_tests =
    {
      tests =
        Option.get (Haz3lcore.TextRoundtrip.of_text ~root:Exp {x|test update_person((first="Thor", age=30, last="Odinson")) 
== 
(first="Thor", age=31, last="Odinson", name="Thor Odinson")
end;
let p = (first="f", age=3, last="l") in
let u = update_person(p) in

test u.age == 4 end;
test u.name == "f l" end;
test to_lvs(u).label == ["first", "age", "last", "name"] end|x});
      hints = ["Example input output matches"; "age field"; "name field"; "tuple order is correct"];
    };
  wrapper = false;
  show_report = true;
}
