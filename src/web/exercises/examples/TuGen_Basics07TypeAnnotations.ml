(* Auto-generated by: ./hazel gen-tutorial -- DO NOT EDIT *)
(* Source: hazel-programs/tutorial/basics/07-type-annotations.hz *)

let exercise : Tutorial.spec = {
  id = Option.get (Haz3lcore.Id.of_string "a0000009-0009-0009-0009-000000000009");
  title = "Basics / 07 - Type Annotations";
  version = 9;
  module_name = "TuGen_Basics07TypeAnnotations";
  prompt = {x|Hazel's type system ensures that expressions are used in ways that make sense. You can explicitly annotate an expression with a type using the `:` operator. For example, `(1 : Int)` asserts that `1` has type `Int`.

If the annotation does not match the expression, Hazel will report a *type error*.

The editor below contains `("1" : Int)`, which has a type error: `"1"` is a `String`, not an `Int`. You will see the type mismatch highlighted in the editor.

Fix the type error by replacing the string `"1"` with the integer `1`. The result should evaluate to `1`.|x};
  display_hint = {x|Fix the type error by using an integer instead of a string|x};
  task_reference = {x|### Type Annotation
```hazel
(42 : Int)
```
```hazel
("hello" : String)
```
```hazel
(true : Bool)
```

### Type annotations on patterns
```hazel
let x : Int = 42 in
let y : String = "hello" in
let z : Bool = true in
(x, y, z)
```

### Basic Types
`Int`, `Float`, `Bool`, `String`|x};
  your_impl =
    Haz3lcore.Zipper.caret_to_start (Option.get (Haz3lcore.TextRoundtrip.of_text ~root:Exp {x|("1" : Int)|x}));
  hidden_tests =
    {
      tests =
        Option.get (Haz3lcore.TextRoundtrip.of_text ~root:Exp {x|test answer == 1 end;
¿|x});
      hints = ["Replace the string `\"1\"` with the integer `1`."];
    };
  wrapper = true;
  show_report = false;
}
