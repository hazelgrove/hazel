(* Auto-generated by: ./hazel gen-tutorial -- DO NOT EDIT *)
(* Source: hazel-programs/tutorial/conjoined/13b-watch-it-build.hz *)

let exercise : Tutorial.spec =
  {
    id =
      Option.get (Haz3lcore.Id.of_string "b0000025-0025-0025-0025-000000000025");
    title = "13b Watch It Build";
    version = 1;
    module_name = "TuGen_13bWatchItBuild";
    prompt =
      {x|A multi-probe shows one value per source row. So how a definition is *formatted* decides how much you see: pack it onto one line for a single value, or break it across lines to watch the intermediates build up.

This `labels` function turns each plant into a sentence. It uses `map` and strings, which you will meet properly soon. Do not worry about every detail; the point here is to probe and reformat.

**Try this**
1. Put your cursor on the `labels` definition and add a probe (**Cmd/Ctrl+E**, or right-click then *Add probe*). The body is one line, so the probe shows one value: the finished list of labels.
2. Each label is built in steps inside the lambda. Break `b.name ++ " needs " ++ string_of_int(b.water * 7) ++ " ml"` across several lines: put the cursor before each `++` and press Enter.
3. New rows appear, and so do their values. The lambda runs once per plant, so each new row shows three samples: the plant `b`, its `name`, the weekly amount `b.water * 7`, and the sentence taking shape.
4. Now join some rows back together. Fewer rows means fewer, coarser values. You choose how much detail to see.|x};
    display_hint =
      {x|Put the cursor just before a `++` and press Enter to break the line; press Backspace to join lines back.|x};
    task_reference =
      {x|### Probes
- **Multi-probe** (on a definition): one probe per row. Break a row to reveal more intermediate values; join rows to see fewer.|x};
    your_impl =
      Haz3lcore.Zipper.caret_to_start
        (Option.get
           (Haz3lcore.TextRoundtrip.of_text ~root:Exp
              {x|type Plant = (name = String, water = Int) in
let labels(beds: [Plant]): [String] =
map(beds, fun b -> b.name ++ " needs " ++ string_of_int(b.water * 7) ++ " ml")
in
labels([
(name = "fern", water = 250),
(name = "orchid", water = 180),
(name = "cactus", water = 50)
])|x}));
    hidden_tests =
      {
        tests =
          Option.get
            (Haz3lcore.TextRoundtrip.of_text ~root:Exp {x|test true end|x});
        hints = [];
      };
    wrapper = false;
    show_report = false;
  }
