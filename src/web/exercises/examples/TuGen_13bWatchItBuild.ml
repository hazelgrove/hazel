(* Auto-generated by: ./hazel gen-tutorial -- DO NOT EDIT *)
(* Source: hazel-programs/tutorial/conjoined/13b-watch-it-build.hz *)

let exercise : Tutorial.spec =
  {
    id =
      Option.get (Haz3lcore.Id.of_string "b0000025-0025-0025-0025-000000000025");
    title = "13b - Watch It Build";
    version = 1;
    module_name = "TuGen_13bWatchItBuild";
    prompt =
      {x|A multi-probe shows one value per source row, so how a definition is *formatted* decides how much you see. Pack it onto one line for a single value, or break it across lines to watch the pieces.

This `amounts` function maps each plant to a watering amount: its base, adjusted by the moon. It uses `map` and a helper `adjust`, which you will meet properly soon. Do not worry about the details; the point here is to probe and reformat.

**Try this**
1. Put your cursor on the `amounts` definition and add a probe (**Cmd/Ctrl+E**, or right-click then *Add probe*). The body is one line, so you see one value: the finished list.
2. Now break the lambda body open. Put a line break after the arrow (`fun p ->`), after the `+`, and after `adjust(p.phase)`.
3. New rows, new values, one per plant. You can read each piece on its own: the plant record `p`, its base `p.base`, the moon adjustment `adjust(p.phase)`, and the total they add up to. There are three plants, so each row shows three samples.
4. Now join the rows back together. Fewer rows means fewer, coarser values. You choose how much detail to see.|x};
    display_hint =
      {x|Put the cursor just after `fun p ->` and press Enter; do the same after the `+`. Press Backspace to join lines back.|x};
    task_reference =
      {x|### Probes
- **Multi-probe** (on a definition): one probe per row. Break a row to reveal more intermediate values; join rows to see fewer.|x};
    your_impl =
      Haz3lcore.Zipper.caret_to_start
        (Option.get
           (Haz3lcore.TextRoundtrip.of_text ~root:Exp
              {x|type MoonPhase = New + Full + Waning in
type Plant = (base = Int, phase = MoonPhase) in
let adjust(phase: MoonPhase): Int =
case phase
| New => 50
| Full => 0 - 30
| _ => 0
end
in
let amounts(plants: [Plant]): [Int] =
map(plants, fun p -> p.base + adjust(p.phase))
in
amounts([
(base = 250, phase = Full),
(base = 180, phase = New),
(base = 50, phase = Waning)
])|x}));
    hidden_tests =
      {
        tests =
          Option.get
            (Haz3lcore.TextRoundtrip.of_text ~root:Exp {x|test true end|x});
        hints = [];
      };
    wrapper = false;
    show_report = false;
  }
