(* Auto-generated by: ./hazel gen-tutorial -- DO NOT EDIT *)
(* Source: hazel-programs/tutorial/conjoined/13-lists.hz *)

let exercise : Tutorial.spec =
  {
    id =
      Option.get (Haz3lcore.Id.of_string "b0000011-0011-0011-0011-000000000011");
    title = "13 - Lists";
    version = 1;
    module_name = "TuGen_13Lists";
    prompt =
      {x|A list holds many values of the same type, written `[a, b, c]`, with type `[T]`.

# Task
Make `garden` the list of these four base waters (`250`, `180`, `50`, `200`), then return how many beds it has with `length`.|x};
    display_hint =
      {x|A list is `[a, b, c, d]`. Count it with `length(garden)`.|x};
    task_reference =
      {x|### Lists
```hazel
[1, 2, 3]
```
A list of integers has type `[Int]`.

### List functions
- `length(xs)` how many elements
- `nth(xs, 0)` the element at an index|x};
    your_impl =
      Haz3lcore.Zipper.caret_to_start
        (Option.get
           (Haz3lcore.TextRoundtrip.of_text ~root:Exp
              {x|let garden: [Int] = ¿ in
¿|x}));
    hidden_tests =
      {
        tests =
          Option.get
            (Haz3lcore.TextRoundtrip.of_text ~root:Exp
               {x|test garden == [250, 180, 50, 200] end;
test answer == 4 end|x});
        hints = [ "garden should be [250, 180, 50, 200]; length(garden) is 4." ];
      };
    wrapper = false;
    show_report = true;
  }
