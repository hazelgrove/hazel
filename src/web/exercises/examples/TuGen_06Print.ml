(* Auto-generated by: ./hazel gen-tutorial -- DO NOT EDIT *)
(* Source: hazel-programs/tutorial/06-print.hz *)

let exercise : Tutorial.spec = {
  id = Option.get (Haz3lcore.Id.of_string "7000000a-7507-4000-8000-000000000000");
  title = "06 Print";
  version = 1;
  module_name = "TuGen_06Print";
  prompt = {x|Work through the inline instructions in the editor below.|x};
  display_hint = {x||x};
  task_reference = {x||x};
  your_impl =
    Option.get (Haz3lcore.TextRoundtrip.of_text ~root:Exp {x|# PROBES TUTORIAL - PART 6: PRINT STATEMENTS #
# Hazel also has traditional print-statement debugging. #
# `print(expr)` sends a value to the print panel in the #
# sidebar, then returns unit. Use `;` to sequence it: #
#   print(something); rest_of_program #

# Look at the sidebar: you should see output from the #
# print statements below. Each entry has a sequence number #
# and a source line number. #

# TRY THIS: Add a `print(n);` line inside the `double` #
# function below to see what values it gets called with. #

let x = 42 in
let y = x + 8 in
print(y);

let double: Int -> Int =
fun n ->
n * 2
in

test double(5) == 10 end;
test double(21) == 42 end;

double(7)

# END OF PART 6 #|x});
  hidden_tests =
    {
      tests =
        Option.get (Haz3lcore.TextRoundtrip.of_text ~root:Exp {x|test true end|x});
      hints = [];
    };
  wrapper = false;
  show_report = false;
}
