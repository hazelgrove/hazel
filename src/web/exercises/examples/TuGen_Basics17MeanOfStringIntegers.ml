(* Auto-generated by: ./hazel gen-tutorial -- DO NOT EDIT *)
(* Source: hazel-programs/tutorial/basics/17-mean-of-string-integers.hz *)

let exercise : Tutorial.spec = {
  id = Option.get (Haz3lcore.Id.of_string "a0000016-0016-0016-0016-000000000016");
  title = "Basics / 17 Mean Of String Integers";
  version = 16;
  module_name = "TuGen_Basics17MeanOfStringIntegers";
  prompt = {x|This task tests your understanding of the concepts from the previous tutorials.

Implement a function:
```hazelnostatics
string_mean : [String] -> Float
```
that takes a list of strings (each representing an integer) and returns their mean as a float.

Some built-in functions and operators you may find useful are listed in the Task Reference sidebar.

**Steps:** Convert the strings to floats, sum them, and divide by the length of the list.|x};
  display_hint = {x|Convert strings to floats with map, sum with fold_left, then divide by length|x};
  task_reference = {x|### Syntax Recap
```hazel
let x : Int = 5 in
x + 1
```
```hazel
fun x -> x + 1
```
```hazel
fun (acc, x) -> acc +. x
```

### Type Conversions
```hazelnostatics
int_of_string : String -> Int
```
```hazelnostatics
float_of_string : String -> Float
```
```hazelnostatics
float_of_int : Int -> Float
```
```hazelnostatics
int_of_float : Float -> Int
```

---

Convert between primitive types using these built-ins:
```hazel
int_of_string("42")
```
```hazel
float_of_string("3.14")
```
```hazel
float_of_int(7)
```
```hazel
int_of_float(3.99)
```

### map
```hazelnostatics
map : ([T], T -> U) -> [U]
```

---

Apply a function to each element:
```hazel
map([1, 2, 3], fun x -> x + 1)
```


### fold_left
```hazelnostatics
fold_left : ([T], (U, T) -> U, U) -> U
```

---

```hazel
fold_left(
  [1, 2, 3],
  fun (acc, x) -> acc + x,
  0
)
```

### length
`length : [T] -> Int` return the length of a list
```hazel
length([1.0, 2.0, 3.0])
```

### Float Arithmetic
- `2.0 +. 3.0` — addition
- `5.0 -. 1.0` — subtraction
- `3.0 *. 2.0` — multiplication
- `6.0 /. 3.0` — division

Float literals need a decimal point: `0.`, `1.0`, `3.14`|x};
  your_impl =
    Option.get (Haz3lcore.TextRoundtrip.of_text ~root:Exp {x|let string_mean : [String] -> Float = fun strings ->
let floats : [Float] = ¿ in
let sum : Float = ¿ in
¿
in
string_mean(["1", "2", "3"])|x});
  hidden_tests =
    {
      tests =
        Option.get (Haz3lcore.TextRoundtrip.of_text ~root:Exp {x|test string_mean(["1", "2", "3"]) == 2. end;
test string_mean(["10"]) == 10. end;
test string_mean(["0", "0", "6"]) == 2. end|x});
      hints = ["string_mean([\"1\", \"2\", \"3\"]) should be 2."; "string_mean([\"10\"]) should be 10."; "string_mean([\"0\", \"0\", \"6\"]) should be 2."];
    };
  wrapper = false;
  show_report = true;
}
