(* Auto-generated by: ./hazel gen-tutorial -- DO NOT EDIT *)
(* Source: hazel-programs/tutorial/basics/14-list-literals.hz *)

let exercise : Tutorial.spec = {
  id = Option.get (Haz3lcore.Id.of_string "a0000012-0012-0012-0012-000000000012");
  title = "Basics / 14 List Literals";
  version = 13;
  module_name = "TuGen_Basics14ListLiterals";
  prompt = {x|In Hazel, you can create a list by writing its elements between square brackets, separated by commas. For example, `[1, 2, 3]` is a list of three integers.

The type of a list is written as `[T]` where `T` is the type of the elements. So `[1, 2, 3]` has type `[Int]`, and `["hello", "world"]` has type `[String]`. All elements in a list must have the same type.

Complete the let binding below so that `first_four` contains the first four positive integers: `[1, 2, 3, 4]`.|x};
  display_hint = {x|Fill in the list with the first four positive integers|x};
  task_reference = {x|### List Literal
```hazelnoeval
[1, 2, 3]
```

### List Type
`[Int]`, `[String]`, `[Bool]`|x};
  your_impl =
    Option.get (Haz3lcore.TextRoundtrip.of_text ~root:Exp {x|let first_four : [Int] = [?] in
first_four|x});
  hidden_tests =
    {
      tests =
        Option.get (Haz3lcore.TextRoundtrip.of_text ~root:Exp {x|test first_four == [1, 2, 3, 4] end|x});
      hints = ["first_four should be [1, 2, 3, 4]"];
    };
  wrapper = false;
  show_report = true;
}
