(* Auto-generated by: ./hazel gen-tutorial -- DO NOT EDIT *)
(* Source: hazel-programs/tutorial/conjoined/06-labelled-tuples.hz *)

let exercise : Tutorial.spec = {
  id = Option.get (Haz3lcore.Id.of_string "b0000016-0016-0016-0016-000000000016");
  title = "06 - Labelled Tuples";
  version = 1;
  module_name = "TuGen_06LabelledTuples";
  prompt = {x|A *labelled tuple* is a kind of *record*. It gives each element a label, so you read it back by name with `.`:
```hazel
let plant = (name = "moonleaf", water = 250) in
plant.name
```

# Task
Complete the `plant` record so its `water` is `250`. The program reads `plant.water`, so it should evaluate to **250**.|x};
  display_hint = {x|Set water to 250.|x};
  task_reference = {x|### Tuple records
```hazel
let plant = (name = "moonleaf", water = 250) in
plant.water
```
Read a field with `.field`. The type is written `(name = String, water = Int)`.|x};
  your_impl =
    Haz3lcore.Zipper.caret_to_start (Option.get (Haz3lcore.TextRoundtrip.of_text ~root:Exp {x|type Plant = (name = String, water = Int) in
let plant: Plant = (name = "moonleaf", water = ¿) in
plant.water|x}));
  hidden_tests =
    {
      tests =
        Option.get (Haz3lcore.TextRoundtrip.of_text ~root:Exp {x|test answer == 250 end|x});
      hints = ["plant.water should be 250."];
    };
  wrapper = true;
  show_report = true;
}
