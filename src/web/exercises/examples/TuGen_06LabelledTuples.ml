(* Auto-generated by: ./hazel gen-tutorial -- DO NOT EDIT *)
(* Source: hazel-programs/tutorial/conjoined/06-labelled-tuples.hz *)

let exercise : Tutorial.spec =
  {
    id =
      Option.get (Haz3lcore.Id.of_string "b0000016-0016-0016-0016-000000000016");
    title = "06 - Labelled Tuples";
    version = 1;
    module_name = "TuGen_06LabelledTuples";
    prompt =
      {x|A *labelled tuple* is a kind of *record*. It gives each element a label, so you read it back by name with `.`:
```hazel
let plant = (name = "moonleaf", water = 250) in
plant.name
```

# Task
Build a `plant` record for a **fern** that drinks **200 ml**, then read its `water` back so the program is **200**.|x};
    display_hint =
      {x|A record is `(field = value, …)`. Match `Plant`'s fields, then read one back with `plant.field`.|x};
    task_reference =
      {x|### Tuple records
```hazel
let plant = (name = "moonleaf", water = 250) in
plant.water
```
Read a field with `.field`. The type is written `(name = String, water = Int)`.|x};
    your_impl =
      Haz3lcore.Zipper.caret_to_start
        (Option.get
           (Haz3lcore.TextRoundtrip.of_text ~root:Exp
              {x|type Plant = (name = String, water = Int) in
let plant: Plant = ¿ in
¿|x}));
    hidden_tests =
      {
        tests =
          Option.get
            (Haz3lcore.TextRoundtrip.of_text ~root:Exp
               {x|test answer == 200 end|x});
        hints =
          [ "Build the fern record, then read plant.water \226\134\146 200." ];
      };
    wrapper = true;
    show_report = true;
  }
