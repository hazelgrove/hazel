(* Auto-generated by: ./hazel gen-tutorial -- DO NOT EDIT *)
(* Source: hazel-programs/tutorial/conjoined/23-greenhouse-arena.hz *)

let exercise : Tutorial.spec =
  {
    id =
      Option.get (Haz3lcore.Id.of_string "b0000023-0023-0023-0023-000000000023");
    title = "23 - Greenhouse Arena";
    version = 1;
    module_name = "TuGen_23GreenhouseArena";
    prompt =
      {x|# The greenhouse

A larger garden, with nothing to fix. The caretaker tends it by moonlight and records every pour in a ledger no one has read to the end.

Auto-probe is already on for this finale (we switched it on for you). Wander: probe a function and step through its samples, **pin** one bed's call and watch the rest fall away, **step into** `weekly` to see a bed totalled night by night. Cycle auto-probe off/on with **Cmd/Ctrl+P** or the **Auto Probe** toggle above.|x};
    display_hint = {x||x};
    task_reference =
      {x|### Everything so far
Probes (Cmd/Ctrl+E), the variable dropdown (right-click a sample), auto-probe (Cmd/Ctrl+P or the **Auto Probe** toggle), many mode (Space or the **Samples** toggle), pin (`P`), step into (Enter), and the sample colors.|x};
    your_impl =
      Haz3lcore.Zipper.caret_to_start
        (Option.get
           (Haz3lcore.TextRoundtrip.of_text ~root:Exp
              {x|type MoonPhase = New + Waxing + Full + Waning in
type Plant = (name = String, water = Int) in

let watering_amount(base: Int, phase: MoonPhase): Int =
case phase
| New => base + 50
| Full => base - 30
| _ => base
end
in

let weekly(plants: [Plant], phase: MoonPhase): Int =
fold_left(plants, fun (acc, p) -> acc + watering_amount(p.water, phase), 0)
in

let fern: Plant = (name = "fern", water = 250) in
let orchid: Plant = (name = "orchid", water = 180) in
let cactus: Plant = (name = "cactus", water = 50) in
let lily: Plant = (name = "lily", water = 200) in

let shade = [fern, lily] in
let sun = [orchid, cactus] in

weekly(shade, Full);
weekly(sun, New);
weekly(shade, Waning)|x}));
    hidden_tests =
      {
        tests =
          Option.get
            (Haz3lcore.TextRoundtrip.of_text ~root:Exp {x|test true end|x});
        hints = [];
      };
    wrapper = false;
    show_report = false;
  }
