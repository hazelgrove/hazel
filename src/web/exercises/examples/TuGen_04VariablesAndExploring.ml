(* Auto-generated by: ./hazel gen-tutorial -- DO NOT EDIT *)
(* Source: hazel-programs/tutorial/conjoined/04-variables-and-exploring.hz *)

let exercise : Tutorial.spec = {
  id = Option.get (Haz3lcore.Id.of_string "b0000004-0004-0004-0004-000000000004");
  title = "04 - Variables And Exploring";
  version = 1;
  module_name = "TuGen_04VariablesAndExploring";
  prompt = {x|Probes are for poking around. Drop them on different bindings to watch a program build up.

# Task
Set the new-moon bonus so the week totals **1800**, then explore.

**Explore**
- Probe `week` to see the subtotal (`1750`).
- Put a probe on the `+` in the `watered` definition, then **right-click that sample** (a two-finger tap on a Mac trackpad, or Option-click) to see a dropdown of the value of variables it contains, including `week`.
- You don't have to use this dropdown; probing `week` directly works just as well. Two ways to see the same thing.|x};
  display_hint = {x|The new-moon bonus is 50.|x};
  task_reference = {x|### Probes
- **Add / remove a probe**: Cmd/Ctrl+E, or right-click then *Add probe*
- **See the variables**: right-click a sample (or Option-click) for the values feeding it|x};
  your_impl =
    Haz3lcore.Zipper.caret_to_start (Option.get (Haz3lcore.TextRoundtrip.of_text ~root:Exp {x|let daily = 250 in
let week = daily * 7 in
let bonus = ¿ in
let watered = week + bonus in
watered|x}));
  hidden_tests =
    {
      tests =
        Option.get (Haz3lcore.TextRoundtrip.of_text ~root:Exp {x|test answer == 1800 end|x});
      hints = ["With the bonus set, the week totals 1800."];
    };
  wrapper = true;
  show_report = true;
}
