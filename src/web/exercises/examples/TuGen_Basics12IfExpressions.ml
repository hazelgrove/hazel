(* Auto-generated by: ./hazel gen-tutorial -- DO NOT EDIT *)
(* Source: hazel-programs/tutorial/basics/12-if-expressions.hz *)

let exercise : Tutorial.spec = {
  id = Option.get (Haz3lcore.Id.of_string "a0000030-0030-0030-0030-000000000030");
  title = "Basics / 12 - If Expressions";
  version = 1;
  module_name = "TuGen_Basics12IfExpressions";
  prompt = {x|If expressions let you choose between two values based on a condition.

The syntax is:
```hazelnostatics
if condition then result1 else result2
```

The condition must be a `Bool`. If it is `true`, the expression evaluates to `result1`; if `false`, to `result2`.

Example:
```hazel
let abs_val = fun x ->
  if x < 0 then 0 - x else x
in
abs_val(-3)
```

You can **nest** if expressions for multiple conditions:
```hazel
let sign = fun x ->
  if x < 0 then -1
  else if x == 0 then 0
  else 1
in
sign(42)
```

# Task

Complete the function `clamp` below.

The function takes an integer `x` and constrains it to the range 0 to 100:

- If `x < 0`, return `0`.
- If `x > 100`, return `100`.
- Otherwise, return `x` unchanged.

**Example**:
```hazelnostatics
clamp(-5) == 0;
clamp(50) == 50;
clamp(150) == 100
```|x};
  display_hint = {x|Use `if x < 0 then 0 else ...` with a nested if for the upper bound|x};
  task_reference = {x|### If Expression
```hazel
let x = -5 in
if x < 0 then 0 - x else x
```

Nested:
```hazel
let x = 5 in
if x < 0 then -1
else if x == 0 then 0
else 1
```

### Comparison Operators
- `==` — equal
- `<` — less than
- `>` — greater than
- `<=` — less than or equal
- `>=` — greater than or equal|x};
  your_impl =
    Haz3lcore.Zipper.caret_to_start (Option.get (Haz3lcore.TextRoundtrip.of_text ~root:Exp {x|let clamp = fun x -> x in
clamp(50)|x}));
  hidden_tests =
    {
      tests =
        Option.get (Haz3lcore.TextRoundtrip.of_text ~root:Exp {x|test clamp(-5) == 0 end;
test clamp(50) == 50 end;
test clamp(150) == 100 end|x});
      hints = ["clamp(-5) should be 0"; "clamp(50) should be 50"; "clamp(150) should be 100"];
    };
  wrapper = false;
  show_report = true;
}
