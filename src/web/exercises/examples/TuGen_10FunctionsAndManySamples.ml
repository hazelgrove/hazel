(* Auto-generated by: ./hazel gen-tutorial -- DO NOT EDIT *)
(* Source: hazel-programs/tutorial/conjoined/10-functions-and-many-samples.hz *)

let exercise : Tutorial.spec = {
  id = Option.get (Haz3lcore.Id.of_string "b0000008-0008-0008-0008-000000000008");
  title = "10 - Functions And Many Samples";
  version = 1;
  module_name = "TuGen_10FunctionsAndManySamples";
  prompt = {x|A function called more than once collects one probe sample *per call*.

`watering_amount` adjusts a base amount by the moon phase. It is called three times below.

**Try this**
1. Probe the function's body: put your cursor on `case` and add a probe.
2. Three samples appear, one per call: `220`, `230`, `50`. Use **←/→** to step between them.
3. Press **Space** for *many* mode to see all three at once. Space again to go back.
4. In *many* mode the samples take on colors: **pink** ran before the sample you click, **blue** ran after. (More on colors near the end.)|x};
  display_hint = {x|Probe the `case`, then use the arrow keys.|x};
  task_reference = {x|### Probes
- **Add / remove a probe**: Cmd/Ctrl+E, or right-click then *Add probe*
- **Many calls**: one sample per call. ←/→ to step, **Space** for all at once.|x};
  your_impl =
    Haz3lcore.Zipper.caret_to_start (Option.get (Haz3lcore.TextRoundtrip.of_text ~root:Exp {x|type MoonPhase = New + Waxing + Full + Waning in
let watering_amount(base: Int, phase: MoonPhase): Int =
case phase
| New => base + 50
| Full => base - 30
| _ => base
end
in
watering_amount(250, Full);
watering_amount(180, New);
watering_amount(50, Waning)|x}));
  hidden_tests =
    {
      tests =
        Option.get (Haz3lcore.TextRoundtrip.of_text ~root:Exp {x|test true end|x});
      hints = [];
    };
  wrapper = false;
  show_report = false;
}
