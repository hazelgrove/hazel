(* Auto-generated by: ./hazel gen-tutorial -- DO NOT EDIT *)
(* Source: hazel-programs/tutorial/conjoined/16-pin.hz *)

let exercise : Tutorial.spec =
  {
    id =
      Option.get (Haz3lcore.Id.of_string "b0000014-0014-0014-0014-000000000014");
    title = "16 - Pin";
    version = 1;
    module_name = "TuGen_16Pin";
    prompt =
      {x|With many calls, every probe shows a wall of samples. **Pin** locks onto one call, so every probe shows just that call's run.

**Try this**
1. Auto-probe is on, so `weekly` is already probed. Switch to *many* mode (press **Space**, or use the **Samples** toggle above): each of its lines shows four samples, one per call below. (If auto-probe is off, probe `weekly` with **Cmd/Ctrl+E** first.)
2. Right-click the `weekly(orchid)` sample below and choose **Pin this call** (or press `P`).
3. Back inside the function, every probe now shows only Orchid's run.
4. A **⍟** marks samples hidden by the pin. Click it to unpin.|x};
    display_hint =
      {x|Right-click a call's sample, then *Pin this call* (or press `P`).|x};
    task_reference =
      {x|### Probes
- **Pin this call**: from a call's sample, filter every probe to that call. Press `P` or use the menu.
- **⍟**: samples hidden by a pin. Click to unpin.|x};
    your_impl =
      Haz3lcore.Zipper.caret_to_start
        (Option.get
           (Haz3lcore.TextRoundtrip.of_text ~root:Exp
              {x|type Plant = (name = String, water = Int) in
let weekly(plant: Plant): Int =
let days = 7 in
plant.water * days
in
let fern: Plant = (name = "fern", water = 250) in
let orchid: Plant = (name = "orchid", water = 180) in
let cactus: Plant = (name = "cactus", water = 50) in
let lily: Plant = (name = "lily", water = 200) in
weekly(fern);
weekly(orchid);
weekly(cactus);
weekly(lily)|x}));
    hidden_tests =
      {
        tests =
          Option.get
            (Haz3lcore.TextRoundtrip.of_text ~root:Exp {x|test true end|x});
        hints = [];
      };
    wrapper = false;
    show_report = false;
  }
