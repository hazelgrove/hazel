(* Auto-generated by: ./hazel gen-tutorial -- DO NOT EDIT *)
(* Source: hazel-programs/tutorial/conjoined/20-print.hz *)

let exercise : Tutorial.spec = {
  id = Option.get (Haz3lcore.Id.of_string "b0000020-0020-0020-0020-000000000020");
  title = "20 - Print";
  version = 1;
  module_name = "TuGen_20Print";
  prompt = {x|Hazel also has classic print-statement debugging. `print(value)` shows a value in the sidebar panel, then carries on. Sequence it with `;`.

# Task
The garden waters for a week. Add `print(daily);` inside `weekly_water` to log each daily amount, then watch the print panel fill as the calls below run.|x};
  display_hint = {x|`print(daily);` before the result, inside the function.|x};
  task_reference = {x|### Print
```hazel
print(42);
"done"
```
`print` writes a value to the sidebar panel and continues. Each entry shows a sequence number and a source line.|x};
  your_impl =
    Haz3lcore.Zipper.caret_to_start (Option.get (Haz3lcore.TextRoundtrip.of_text ~root:Exp {x|let weekly_water(daily: Int): Int =
daily * 7
in
weekly_water(250);
weekly_water(180);
weekly_water(50)|x}));
  hidden_tests =
    {
      tests =
        Option.get (Haz3lcore.TextRoundtrip.of_text ~root:Exp {x|test true end|x});
      hints = [];
    };
  wrapper = false;
  show_report = false;
}
