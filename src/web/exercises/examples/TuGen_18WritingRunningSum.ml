(* Auto-generated by: ./hazel gen-tutorial -- DO NOT EDIT *)
(* Source: hazel-programs/tutorial/conjoined/18-writing-running-sum.hz *)

let exercise : Tutorial.spec =
  {
    id =
      Option.get (Haz3lcore.Id.of_string "b0000018-0018-0018-0018-000000000018");
    title = "18 - Writing Running Sum";
    version = 1;
    module_name = "TuGen_18WritingRunningSum";
    prompt =
      {x|Now write one yourself. **Auto-probe is already on for this slide** (we switched it on for you) so you can watch it run.

`nightly_totals` takes each night's watering and returns the *running total* so far:
```hazelnostatics
nightly_totals([250, 180, 50]) == [250, 430, 480]
```

# Task
Fill the fold callback. The accumulator is a tuple `(total, sofar)`: the running total, and the list built so far. Each step returns the new total and `sofar` with that total added to the end. Some useful list operators: `::` (cons) and `@` (append).

Auto-probe covers the callback, so in *many* mode (press **Space**, or use the **Samples** toggle above) you can watch the accumulator grow. Cycle auto-probe off/on any time with **Ctrl/Cmd+P** or the **Auto Probe** toggle above.|x};
    display_hint =
      {x|Return a pair: the new total, and `sofar` with that new total appended (with `@`).|x};
    task_reference =
      {x|### Fold with a tuple accumulator
```hazel
fold_left([1, 2, 3], fun ((sum, sofar), n) ->
  (sum + n, sofar @ [sum + n])
, (0, []))
```
`xs @ ys` joins two lists (so does `append(xs, ys)`); `x :: xs` adds to the front. Take the second part with `let (_, result) = ... in result`.|x};
    your_impl =
      Haz3lcore.Zipper.caret_to_start
        (Option.get
           (Haz3lcore.TextRoundtrip.of_text ~root:Exp
              {x|let nightly_totals(nights: [Int]): [Int] =
let (_, result) =
fold_left(
nights,
fun ((total, sofar), n) ->
¿
,(0, [])
)
in
result
in
nightly_totals([250, 180, 50])|x}));
    hidden_tests =
      {
        tests =
          Option.get
            (Haz3lcore.TextRoundtrip.of_text ~root:Exp
               {x|test nightly_totals([250, 180, 50]) == [250, 430, 480] end;
test nightly_totals([5]) == [5] end;
test nightly_totals([]) == [] end|x});
        hints = [ "nightly_totals([250, 180, 50]) should be [250, 430, 480]." ];
      };
    wrapper = false;
    show_report = true;
  }
