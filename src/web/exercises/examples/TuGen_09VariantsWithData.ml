(* Auto-generated by: ./hazel gen-tutorial -- DO NOT EDIT *)
(* Source: hazel-programs/tutorial/conjoined/09-variants-with-data.hz *)

let exercise : Tutorial.spec = {
  id = Option.get (Haz3lcore.Id.of_string "b0000017-0017-0017-0017-000000000017");
  title = "09 - Variants With Data";
  version = 1;
  module_name = "TuGen_09VariantsWithData";
  prompt = {x|A variant can carry data. Give a constructor a payload in its type, and bind that payload in the matching pattern:
```hazelnostatics
type Care = Water(Int) + Trim + Skip in ...
```
Matching `Water(amount)` pulls the carried value into `amount`. This is how the garden's actions are modeled.

# Task
Complete the `Water` branch so watering adds `amount` to the base. `apply(250, Water(50))` should be **300**.|x};
  display_hint = {x|`base + amount`|x};
  task_reference = {x|### Variants with data
```hazel
type Care = Water(Int) + Skip in
case Water(50)
| Water(n) => n
| Skip => 0
end
```

### Option
`Option` is the built-in variant for "maybe a value": `Some(value)` or `None`.
```hazel
case Some(7)
| Some(n) => n
| None => 0
end
```|x};
  your_impl =
    Haz3lcore.Zipper.caret_to_start (Option.get (Haz3lcore.TextRoundtrip.of_text ~root:Exp {x|type Care = Water(Int) + Trim + Skip in
let apply(base: Int, action: Care): Int =
case action
| Water(amount) => ¿
| Trim => base - 10
| Skip => base
end
in
apply(250, Water(50))|x}));
  hidden_tests =
    {
      tests =
        Option.get (Haz3lcore.TextRoundtrip.of_text ~root:Exp {x|test answer == 300 end|x});
      hints = ["apply(250, Water(50)) should be 300."];
    };
  wrapper = true;
  show_report = true;
}
