(* Auto-generated by: ./hazel gen-tutorial -- DO NOT EDIT *)
(* Source: hazel-programs/tutorial/conjoined/21-debugging-warmup.hz *)

let exercise : Tutorial.spec = {
  id = Option.get (Haz3lcore.Id.of_string "b0000021-0021-0021-0021-000000000021");
  title = "21 - Debugging Warmup";
  version = 1;
  module_name = "TuGen_21DebuggingWarmup";
  prompt = {x|Every debugging task in the study shares one shape: a list of **actions** folded into a **model**. Here is a tiny one, and it has a bug.

A bed starts at water level `100`. `Water(n)` adds `n`; `Drain` should remove `20`. The bed runs through a list of actions with `fold_left`.

# Task
One test fails. Turn on auto-probe, step into `run`, and watch the level after each action to find the branch that moves the wrong way. Fix it so the tests pass.|x};
  display_hint = {x|Walk the levels from 100. Which action changes it in the wrong direction?|x};
  task_reference = {x|### Folding actions into a model
```hazel
fold_left(actions, update, start_model)
```
`update(model, action)` returns the next model, and the fold carries it forward. This is the pattern behind the study's debugging tasks.|x};
  your_impl =
    Haz3lcore.Zipper.caret_to_start (Option.get (Haz3lcore.TextRoundtrip.of_text ~root:Exp {x|type Action = Water(Int) + Drain in
type Bed = (level = Int) in
let update(bed: Bed, action: Action): Bed =
case action
| Water(n) => (level = bed.level + n)
| Drain => (level = bed.level + 20)
end
in
let run(actions: [Action]): Bed =
fold_left(actions, update, (level = 100))
in
run([Water(50), Drain, Water(30)])|x}));
  hidden_tests =
    {
      tests =
        Option.get (Haz3lcore.TextRoundtrip.of_text ~root:Exp {x|test run([Water(50), Drain, Water(30)]).level == 160 end;
test run([Drain]).level == 80 end;
test run([]).level == 100 end|x});
      hints = ["From 100, the actions Water(50), Drain, Water(30) should end at 160."];
    };
  wrapper = false;
  show_report = true;
}
