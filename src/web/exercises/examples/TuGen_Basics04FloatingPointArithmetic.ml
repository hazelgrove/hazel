(* Auto-generated by: ./hazel gen-tutorial -- DO NOT EDIT *)
(* Source: hazel-programs/tutorial/basics/04-floating-point-arithmetic.hz *)

let exercise : Tutorial.spec = {
  id = Option.get (Haz3lcore.Id.of_string "a0000004-0004-0004-0004-000000000004");
  title = "Basics / 04 - Floating Point Arithmetic";
  version = 4;
  module_name = "TuGen_Basics04FloatingPointArithmetic";
  prompt = {x|Hazel distinguishes between integer and floating-point arithmetic. Floating-point operators are written with a `.` suffix:
- `+.` addition
- `-.` subtraction
- `*.` multiplication
- `/.` division

Float literals must include a decimal point, e.g. `3.14`, `1.0`, or `1.`. This distinction ensures type safety — you can't accidentally mix integer and float operations.

**Note:** a digit must appear before the decimal point (`0.5`, not `.5`).

The editor below contains `3.0 * 2.0`, which uses the *integer* multiplication operator `*` on float values — causing a type error. Fix it by replacing `*` with the floating-point multiplication operator.|x};
  display_hint = {x|Remember: float operators end with `.`|x};
  task_reference = {x|### Integer Arithmetic
- `2 + 3` — addition
- `5 - 1` — subtraction
- `4 * 3` — multiplication
- `10 / 3` — integer division

### Float Arithmetic
- `2.0 +. 3.0` — addition
- `5.0 -. 1.0` — subtraction
- `3.0 *. 2.0` — multiplication
- `6.0 /. 3.0` — division

Float literals need a decimal point: `0.`, `1.0`, `3.14`|x};
  your_impl =
    Haz3lcore.Move.to_start (Option.get (Haz3lcore.TextRoundtrip.of_text ~root:Exp {x|3.0 * 2.0|x}));
  hidden_tests =
    {
      tests =
        Option.get (Haz3lcore.TextRoundtrip.of_text ~root:Exp {x|test answer ==. 6. end;
¿|x});
      hints = ["Use `3.0 *. 2.0` with the float multiplication operator `*.`."];
    };
  wrapper = true;
  show_report = false;
}
