(* Auto-generated by: ./hazel gen-tutorial -- DO NOT EDIT *)
(* Source: hazel-programs/tutorial/conjoined/14-map.hz *)

let exercise : Tutorial.spec =
  {
    id =
      Option.get (Haz3lcore.Id.of_string "b0000012-0012-0012-0012-000000000012");
    title = "14 - Map";
    version = 1;
    module_name = "TuGen_14Map";
    prompt =
      {x|`map` applies a function to every element of a list. Write the function inline as a *lambda* with `fun`:
```hazelnostatics
map([1, 2, 3], fun n -> n * 2)
```

# Task

Just for fun, turn on auto-probe (**Ctrl/Cmd+P**, or the **Auto Probe** toggle at the top of this panel) if it's not already on. Then, give every plant its 50 ml new-moon top-up. Map over the watering cans, adding 50 to each.|x};
    display_hint =
      {x|Write a lambda `fun w -> …` that adds 50 to each element.|x};
    task_reference =
      {x|### Map and lambdas
```hazel
map([1, 2, 3], fun n -> n + 1)
```
`fun n -> ...` is a function with no name.

### More list functions
- `filter(xs, fun x -> x > 100)` keeps the elements that pass
- `mapi(xs, fun (i, x) -> ...)` like map, but also gives the index `i`|x};
    your_impl =
      Haz3lcore.Zipper.caret_to_start
        (Option.get
           (Haz3lcore.TextRoundtrip.of_text ~root:Exp
              {x|let watering_cans = [250, 180, 50, 200] in
map(watering_cans, ¿)|x}));
    hidden_tests =
      {
        tests =
          Option.get
            (Haz3lcore.TextRoundtrip.of_text ~root:Exp
               {x|test answer == [300, 230, 100, 250] end|x});
        hints = [ "Each watering cans gains 50: [300, 230, 100, 250]." ];
      };
    wrapper = true;
    show_report = true;
  }
