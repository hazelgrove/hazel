(* Auto-generated by: ./hazel gen-tutorial -- DO NOT EDIT *)
(* Source: hazel-programs/tutorial/conjoined/19-writing-strings.hz *)

let exercise : Tutorial.spec = {
  id = Option.get (Haz3lcore.Id.of_string "b0000019-0019-0019-0019-000000000019");
  title = "19 - Writing Strings";
  version = 1;
  module_name = "TuGen_19WritingStrings";
  prompt = {x|The garden keeps a log. Text comes apart with string functions, and `string_match` tests a pattern against a string (a small regex).

# Task
Keep only the log entries that mention a bloom. Fill the test so `filter` keeps the entries containing `"bloom"`.|x};
  display_hint = {x|`string_match("bloom", e)`|x};
  task_reference = {x|### Strings
- `string_match(pattern, s)` is `true` if the pattern occurs in `s`. Patterns are regexes, e.g. `"bloom"` or `"#[0-9]+"`.
- `string_split(" ", s)` breaks `s` into pieces
- `string_length(s)`, `string_sub(s, start, len)`|x};
  your_impl =
    Haz3lcore.Zipper.caret_to_start (Option.get (Haz3lcore.TextRoundtrip.of_text ~root:Exp {x|let entries = ["moonbloom ok", "cactus dry", "starbloom ok"] in
filter(entries, fun e -> ¿)|x}));
  hidden_tests =
    {
      tests =
        Option.get (Haz3lcore.TextRoundtrip.of_text ~root:Exp {x|test answer == ["moonbloom ok", "starbloom ok"] end|x});
      hints = ["Keep \"moonbloom ok\" and \"starbloom ok\"."];
    };
  wrapper = true;
  show_report = true;
}
