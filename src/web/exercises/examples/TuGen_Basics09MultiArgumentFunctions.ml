(* Auto-generated by: ./hazel gen-tutorial -- DO NOT EDIT *)
(* Source: hazel-programs/tutorial/basics/09-multi-argument-functions.hz *)

let exercise : Tutorial.spec = {
  id = Option.get (Haz3lcore.Id.of_string "a0000007-0007-0007-0007-000000000007");
  title = "Basics / 09 - Multi Argument Functions";
  version = 8;
  module_name = "TuGen_Basics09MultiArgumentFunctions";
  prompt = {x|Hazel does NOT support currying but supports multi-argument functions via tuples:

```hazelnoeval
fun (x, y) -> x + y
```
is a function that takes two arguments and sums them. You apply a multi-argument function by passing a tuple:

```hazel
(fun (x, y) -> x + y)(1, 2)
```


# Task

Define a `perimeter` function below that takes two arguments — `width` and `height` — and returns the perimeter of a rectangle: `2 * (width + height)`.|x};
  display_hint = {x|Insert a function with params (width, height)|x};
  task_reference = {x|### Multi-Argument Function
```hazel
fun (x, y) -> x + y
```

### Binding and Calling
```hazel
let add : (Int, Int) -> Int =
fun (x, y) -> x + y 
in
add(3, 4)
```|x};
  your_impl =
    Option.get (Haz3lcore.TextRoundtrip.of_text ~root:Exp {x|let perimeter : (Int, Int) -> Int = ¿ in
perimeter(3, 4)|x});
  hidden_tests =
    {
      tests =
        Option.get (Haz3lcore.TextRoundtrip.of_text ~root:Exp {x|test perimeter(1, 1) == 4 end;
test perimeter(5, 10) == 30 end;
test perimeter(10, 2) == 24 end|x});
      hints = ["perimeter(1, 1) should be 4"; "perimeter(5, 10) should be 30"; "perimeter(10, 2) should be 24"];
    };
  wrapper = false;
  show_report = true;
}
