(* Auto-generated by: ./hazel gen-tutorial -- DO NOT EDIT *)
(* Source: hazel-programs/tutorial/basics/01-holes.hz *)

let exercise : Tutorial.spec = {
  id = Option.get (Haz3lcore.Id.of_string "a0000001-0001-0001-0001-000000000001");
  title = "Basics / 01 - Holes";
  version = 1;
  module_name = "TuGen_Basics01Holes";
  prompt = {x|# Welcome to Hazel!

Hazel is a live functional programming environment where every edit state is a valid program.

Hazel achieves this using *holes*. When part of a program is missing or contains a parse error, Hazel inserts a hole as a placeholder. Holes are displayed as hexagons in the editor, and Hazel can evaluate around them.

Your goal is to fill the empty hole in the program below so that the arithmetic expression evaluates to **42**.|x};
  display_hint = {x|The answer is 42 - 20|x};
  task_reference = {x|This is the Task Reference sidebar, where you can find reference material as you work through the tasks. It includes operations we think may be helpful.

### Useful arithmetic
```hazel
42 - 20
```|x};
  your_impl =
    Option.get (Haz3lcore.TextRoundtrip.of_text ~root:Exp {x|let target = 42 in
let partial = 20 + ¿ in
target == partial|x});
  hidden_tests =
    {
      tests =
        Option.get (Haz3lcore.TextRoundtrip.of_text ~root:Exp {x|test answer end|x});
      hints = ["Replace the hole with `22` so that `partial == 42`."];
    };
  wrapper = true;
  show_report = false;
}
