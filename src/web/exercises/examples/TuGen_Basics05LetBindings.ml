(* Auto-generated by: ./hazel gen-tutorial -- DO NOT EDIT *)
(* Source: hazel-programs/tutorial/basics/05-let-bindings.hz *)

let exercise : Tutorial.spec = {
  id = Option.get (Haz3lcore.Id.of_string "a0000005-0005-0005-0005-000000000005");
  title = "Basics / 05 Let Bindings";
  version = 6;
  module_name = "TuGen_Basics05LetBindings";
  prompt = {x|A `let` expression binds a variable to a value within a body expression.

The syntax is:
```hazelnostatics
let x = expr in
body
```

where `x` is available for use in `body`.

Example:
```hazel
let h = "Hello" in
let w = "World" in
let space = " " in
h ++ space ++ w
```

# Task

The editor below calculates the total cost of a meal, where the tip should be 20% of the price. The `tip` variable is currently undefined — define it as 20% of `price`.|x};
  display_hint = {x|Use floating point multiplication and a let binding to add the tip|x};
  task_reference = {x|### Let Expression
```hazel
let x = 5 in
let y = x + 1 in
y
```

Variables bound by `let` are available in the body after `in`.

### Float Arithmetic
- `2.0 +. 3.0` — addition
- `5.0 -. 1.0` — subtraction
- `3.0 *. 2.0` — multiplication
- `6.0 /. 3.0` — division

Float literals need a decimal point: `0.`, `1.0`, `3.14`|x};
  your_impl =
    Option.get (Haz3lcore.TextRoundtrip.of_text ~root:Exp {x|let price : Float = 50. in
let total = price +. tip in
total|x});
  hidden_tests =
    {
      tests =
        Option.get (Haz3lcore.TextRoundtrip.of_text ~root:Exp {x|test tip ==. 10. end;
test total ==. 60. end|x});
      hints = ["tip should be 10."; "total should be 60."];
    };
  wrapper = false;
  show_report = true;
}
