(library
 (name util)
 (flags
  (:standard -open Core))
 (libraries
  core
  ptmap
  bonsai.web
  virtual_dom.input_widgets
  bignum
  uuidm
  unionFind
  csv)
 (js_of_ocaml)
 (instrumentation
  (backend bisect_ppx))
 (preprocess
  (pps
   ppx_yojson_conv
   js_of_ocaml-ppx
   ppx_let
   ppx_sexp_conv
   ppx_deriving.show
   ppx_deriving.eq
   bonsai.ppx_bonsai)))

(env
 (dev
  (js_of_ocaml
   (flags :standard --debuginfo --noinline --dynlink --linkall --sourcemap)))
 (release
  (js_of_ocaml
   (flags :standard))))
