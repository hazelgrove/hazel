/** Pretty-printing. */;

open Lang;

/** Pretty-prints a pattern. */

let pat: pat => string;

/** Pretty-prints a type. */

let typ: typ => string;

/** Pretty-prints an expression. */

let exp: exp => string;
