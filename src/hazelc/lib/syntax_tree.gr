export enum BinBoolOp {
    And,
    Or,
}

export enum DHExp {
    EmptyHole(Number),
    NonEmptyHole(
      Number,
      DHExp,
    ),
    Ap(DHExp, DHExp),
    BoolLit(Bool),
    BinBoolOp(BinBoolOp, DHExp, DHExp),
}
