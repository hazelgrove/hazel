import Ast from "./ast"

export enum MaybeIndet<t> {
  Value(t),
  Indet(Ast.Ast),
}

export let is_value = m =>
  match (m) {
    Value(_) => true,
    Indet(_) => false,
  }

export let is_indet = m =>
  match (m) {
    Value(_) => false,
    Indet(_) => true,
  }
