(include_subdirs unqualified)

(executable
 (name cli)
 (libraries ppx_yojson_conv.expander haz3lcore cmdliner unix)
 (modes js)
 (preprocess
  (pps js_of_ocaml-ppx ppx_deriving.show)))

(env
 (dev
  (js_of_ocaml
   (flags :standard --debuginfo --noinline --dynlink --linkall --sourcemap)))
 (release
  (js_of_ocaml
   (flags (:standard)))))
