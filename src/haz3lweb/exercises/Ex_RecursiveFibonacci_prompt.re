// open Virtual_dom.Vdom;
// open Node;
// open ExerciseUtil;

let prompt = "Write test cases for, and then implement, a function that recursively determines the nth Fibonacci number. \n`fib(n)` is equivalent to the `n`th Fibonacci number, assuming `n >= 0`.";
