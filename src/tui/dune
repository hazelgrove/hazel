(include_subdirs unqualified)

(library
 (name haz3ltui)
 (modules
  (:standard \ Tui NottyIO NottyEvents NottyAttr))
 (libraries haz3lcore unix)
 (preprocess
  (pps ppx_deriving.show ppx_yojson_conv ppx_sexp_conv)))

(executable
 (name tui)
 (modules Tui NottyIO NottyEvents NottyAttr)
 (libraries haz3ltui notty notty.unix)
 (modes exe))

(env
 (dev
  (js_of_ocaml
   (flags :standard --debuginfo --noinline --dynlink --linkall --sourcemap)))
 (release
  (js_of_ocaml
   (flags (:standard)))))
