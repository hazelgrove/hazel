# Update Playlist app model based on an action #

let update: (PlayList, PlayListAction) -> PlayList =
  
fun model, action -> 
  case action of
  | AddPlaylist(title)    => title::model 
  | RemovePlaylist(title) => List.remove(title, model)
  end
in