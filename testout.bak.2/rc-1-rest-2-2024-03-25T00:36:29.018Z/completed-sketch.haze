# Update Playlist app model based on an action #

let update: (PlayList, PlayListAction) -> PlayList =
  fun (pl, act) ->
    case act
    | Pause => {pl with state = Paused}
    | Play => {pl with state = Playing(get_songs(pl))}
    | Stop => {pl with state = Stopped}
    end
in