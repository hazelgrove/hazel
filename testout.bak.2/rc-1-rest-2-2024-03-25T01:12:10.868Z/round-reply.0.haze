fun model, action ->
  case action
  | ClearGrid => {model with grid = clearGrid(model.grid)}
  | FillRow(row, emoji) => {model with grid = fillRowInGrid(model.grid, row, emoji)}
  | UpdateCell(row, col, emoji) => {model with grid = updateGrid(model.grid, row, col, emoji)}
  end