fun model, action ->
  case action
  | AddEmoji(new_emoji) => {model with emojis = new_emoji::model.emojis}
  | DeleteEmoji(idx) => {model with emojis = List.remove_at(idx, model.emojis)}
  | ModifyEmoji(idx, new_emoji) => {model with emojis = List.update_at(idx, new_emoji, model.emojis)}
  end