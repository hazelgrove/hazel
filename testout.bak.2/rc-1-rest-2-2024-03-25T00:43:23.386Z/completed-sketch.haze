# Update Playlist app model based on an action #

let update: (PlayList, PlayListAction) -> PlayList =
  fun (m, act) ->
  case act
    | Resume => m
    | Pause => m
    | Play(id) => m
    | Stop => m
    | Add(id) =>  m
    | Remove(id) => m
  end
in