# Update Playlist app model based on an action #

let update: (PlayList, PlayListAction) -> PlayList =
  
fun playlist, action ->
  case action
  | AddTrack(track) => playlist.addTrack(track)
  | RemoveTrack(track) => playlist.removeTrack(track)
  | ClearPlaylist => playlist.clear()
  end
in