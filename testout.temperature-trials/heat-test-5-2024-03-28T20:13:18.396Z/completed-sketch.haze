# Update Playlist app model based on an action #

let update: (PlayList, PlayListAction) -> PlayList =
  fun (playlist, action) ->
  case action
  | Add(track) => List.append(playlist, [track])
  | Remove(track) => List.filter(fun t -> t != track, playlist)
  | Clear => []
  end
in