# Update Playlist app model based on an action #

let update: (PlayList, PlayListAction) -> PlayList =
  fun (state, action) ->
  case action
  | AddTrack(track) => List.append(state, [track])
  | RemoveTrack(track) => List.filter(fun t -> t != track, state)
  | ClearPlaylist => []
  end
in