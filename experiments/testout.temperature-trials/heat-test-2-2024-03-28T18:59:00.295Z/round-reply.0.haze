fun model, action ->
  case action
  | AddTodo => add(model)
  | RemoveTodo(index) => let (buffer, todos) = model in (buffer, remove(index, todos))
  | ToggleTodo(index) => let (buffer, todos) = model in (buffer, toggle(index, todos))
  | UpdateBuffer(text) => let (_, todos) = model in (text, todos)
  end