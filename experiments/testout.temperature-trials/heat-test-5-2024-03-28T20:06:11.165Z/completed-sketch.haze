# Update Playlist app model based on an action #

let update: (PlayList, PlayListAction) -> PlayList =
  fun (state, action) ->
    case action
      | AddSong(song) => List.append(state, [song])
      | RemoveSong(song) => List.filter(fun s -> s != song, state)
    end
in