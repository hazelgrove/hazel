# Update Playlist app model based on an action #

let update: (PlayList, PlayListAction) -> PlayList =
  fun model, action ->
  case action
  | AddSong(song) => model @ [song]
  | RemoveSong(song) => List.filter(fun s -> s != song, model)
  | Shuffle => List.shuffle(model)
  | Clear => [] end
in