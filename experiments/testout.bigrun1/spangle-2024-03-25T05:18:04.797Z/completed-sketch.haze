# TODO MVU: Handle TODO actions to update the app model #

let update: (Model, Action) -> Model =
  
fun model, action ->
    case action
      | Add => { model with counter = model.counter + 1 }
      | Remove => { model with counter = model.counter - 1 }
    end
in
