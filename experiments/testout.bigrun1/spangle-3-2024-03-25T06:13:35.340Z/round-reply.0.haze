fun m, a ->
case a
| AddBooking(user, weekday, timeOfDay) =>
  let (formData, bookings, nextID) = m in
  ((weekday, timeOfDay, user), bookings, nextID)
| CancelBooking(user, id) =>
  let (formData, bookings, nextID) = m in
  (formData, List.filter (fun (_, _, bID) -> bID != id) bookings, nextID)
| ClearBookings =>
  let (formData, _, nextID) = m in
  (formData, [], nextID)
end