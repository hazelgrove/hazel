# TODO MVU: Handle TODO actions to update the app model #

let update: (Model, Action) -> Model =
  fun (model, action) ->
  case action
  | Increase => { count = model.count + 1 }
  | Decrease => { count = model.count - 1 }
  | Reset => { count = 0 }
  end
in
