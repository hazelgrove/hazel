# Update Playlist app model based on an action #

let update: (PlayList, PlayListAction) -> PlayList =
  fun (model, action) ->
    case action
    | Add(track) => model.add(track)
    | Delete(track) => model.delete(track)
    | Clear => model.clear()
    end
in