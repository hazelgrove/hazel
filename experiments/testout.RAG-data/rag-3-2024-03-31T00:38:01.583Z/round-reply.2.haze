fun (grid, current_emoji), action ->
  case action
  | SelectEmoji(new_emoji) => (grid, new_emoji)
  | StampEmoji(row, col) => (updateGrid(grid, row, col, current_emoji), current_emoji)
  | FillRow(row) => (fillRow(grid, row, current_emoji), current_emoji) 
  end