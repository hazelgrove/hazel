fun (playlist, action) ->
    case action
    | PlaySong(id) => ([id], Playing(id))
    | PauseCurrentSong => (playlist.1, PausedOn(playlist.1))
    | RemoveSong(id) => (List.filter((fun song_id -> song_id != id), playlist.1), playlist.2)
    | AddSong(id) => (id::playlist.1, playlist.2)
    end