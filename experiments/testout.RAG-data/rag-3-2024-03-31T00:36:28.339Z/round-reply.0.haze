fun model, action ->
  case action
  | AddBooking(user, weekday, time) => if !bookingExists(model, (time, user)) then model else addBooking(model, (time, user))
  | RemoveBooking(id) => removeBooking(model, id)
  | UpdateForm(userAndTime) => updateForm(model, userAndTime)  
  end