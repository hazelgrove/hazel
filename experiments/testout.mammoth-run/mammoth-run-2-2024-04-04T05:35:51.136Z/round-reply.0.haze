fun model, action ->
  case action
  | AddSong(song) => AddSongToPlayList(model, song)
  | RemoveSong(song) => RemoveSongFromPlayList(model, song)
  | Play => PlayPlayList(model)
  | Pause => PausePlayList(model)
  | Stop => StopPlayList(model)
  end