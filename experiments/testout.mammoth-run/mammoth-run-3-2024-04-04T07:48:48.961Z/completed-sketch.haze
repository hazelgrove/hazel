# TODO MVU: Handle TODO actions to update the app model #

let update: (Model, Action) -> Model =
  
fun m, a ->
  case a
    | AddTodo(t) => {m with todos = add(m)}
    | RemoveTodo(i) => {m with todos = remove(i, m.todos)}
    | ToggleTodo(i) => {m with todos = toggle(i, m.todos)}
  end
in
