
fun model, action ->
  case action
  | AddBooking(user, weekday, time) => addBooking(model, user, weekday, time)
  | RemoveBooking(bookingID) => removeBooking(model, bookingID)
  | UpdateBuffer(buffer) => updateBuffer(model, buffer)
  end