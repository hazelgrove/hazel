
fun model, action ->
  case action
  | AddEmoji(emoji) => { model with emojis = emoji :: model.emojis }
  | Clear => { model with emojis = [] }
  | SetEmojiSize(size) => { model with emojiSize = size }
  end