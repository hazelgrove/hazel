fun (model, action) ->
    case action
    | AddBooking(user, weekday, time) => if bookingExists(model, (time, user, 0)) then model else addBooking(model, (time, user, 0))
    | RemoveBooking(id) => removeBooking(model, id)
    | UpdateBuffer(string) => updateBuffer(model, string)
    end