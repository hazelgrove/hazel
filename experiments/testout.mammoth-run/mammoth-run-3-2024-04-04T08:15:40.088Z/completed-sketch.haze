# Update Playlist app model based on an action #

let update: (PlayList, PlayListAction) -> PlayList =
  fun model, action ->
    case action
    | AddTrack(track) => model |> addTrack(track)
    | RemoveTrack(track) => model |> removeTrack(track)
    | ClearPlayList => emptyPlayList
    end
in