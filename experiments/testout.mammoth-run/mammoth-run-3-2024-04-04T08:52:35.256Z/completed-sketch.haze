# Update the EmojiPaint app model based on an action #

let update: (Model, Action) -> Model =
  fun (grid, action) ->
  case action
    | SelectEmoji(emoji) => (emoji, grid)
    | StampEmoji(row, col) => updateGrid(grid, row, col, emoji)
    | FillRow(row) => fillRow(grid, row, emoji)
  end
in
