fun (model, action) ->
  case action
    | AddTodo => addTodoToModel(model)
    | RemoveTodo(index) => removeTodoFromModel(model, index)
    | ToggleTodo(index) => toggleTodoInModel(model, index)
    | UpdateBuffer(text) => updateBufferInModel(model, text)
  end