# TODO MVU: Handle TODO actions to update the app model #

let update: (Model, Action) -> Model =
  fun model, action ->
    case action
    | AddTodo => let (buffer, todos) = model in (buffer, add(buffer, todos))
    | RemoveTodo(idx) => let (buffer, todos) = model in (buffer, remove(idx, todos))
    | ToggleTodo(idx) => let (buffer, todos) = model in (buffer, toggle(idx, todos))
    | UpdateBuffer(buffer) => let (_, todos) = model in (buffer, todos)
    end
in
