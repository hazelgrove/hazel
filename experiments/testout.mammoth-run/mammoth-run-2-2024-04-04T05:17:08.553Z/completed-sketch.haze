# TODO MVU: Handle TODO actions to update the app model #

let update: (Model, Action) -> Model =
  fun model, action ->
  case action
  | Add => Model.init
  | Toggle(id) => toggle(id, model.todos)
  | Remove(id) => remove(id, model.todos)
  end
in
