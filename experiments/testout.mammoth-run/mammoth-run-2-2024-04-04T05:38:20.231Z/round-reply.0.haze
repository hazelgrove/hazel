fun model, action ->
  case action
    | AddSong(song) => add_song(model, song)
    | RemoveSong(song) => remove_song(model, song)
    | PlaySong => play_song(model)
    | StopSong => stop_song(model)
  end