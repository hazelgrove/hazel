fun model, action ->
  case action
  | AddBooking(time, user, id) => if !bookingExists(model, (time, user, id)) then model else addBooking(model, (time, user, id))
  | RemoveBooking(id) => removeBooking(model, id)
  | UpdateBuffer(str) => updateBuffer(model, str)
  end