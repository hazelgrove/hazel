# TODO MVU: Handle TODO actions to update the app model #

let update: (Model, Action) -> Model =
  fun (model, action) ->
  case action
  | AddTodo => add(model)
  | RemoveTodo(i) => remove(model,i)
  | ToggleTodo(i) => toggle(model,i)
  | UpdateBuffer(s) => update_buffer(model,s)
  end
in
