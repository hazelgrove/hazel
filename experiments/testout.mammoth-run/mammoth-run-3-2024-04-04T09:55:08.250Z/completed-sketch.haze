# Update Password Strength Checker app model based on an action #

let update: (Model, Action) -> Model =
  
fun model, action ->
  case action
  | Change(new_password) => { model with password = new_password; strength = calculate_strength(new_password) }
  | Submit => { model with submitted = true }
  end
in