fun (playlist, action) ->
  case action
    | PlaySong(id) => (playlist[0], Playing(id))
    | PauseCurrentSong => (playlist[0], PausedOn(List.hd(playlist[0])))
    | RemoveSong(id) => (List.filter(fun song_id -> song_id != id, playlist[0]), playlist[1])
    | AddSong(id) => ([id] @ playlist[0], playlist[1])
  end