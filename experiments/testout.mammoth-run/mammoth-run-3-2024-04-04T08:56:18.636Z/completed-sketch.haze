# Update the EmojiPaint app model based on an action #

let update: (Model, Action) -> Model =
  fun (model, action) ->
    case action
    | SelectEmoji(emoji) => (model, emoji)
    | StampEmoji(row, col) => updateGrid(model, row, col, model)
    | FillRow(row) => fillRow(model, row)
    end
in
