# Update the Room Booking app model based on an action #

let update: (Model, Action) -> Model =
  fun (model, action) ->
  case action
  | BookRoom(room) => { rooms = List.append(model.rooms, [room]) }
  | CancelBooking(room) => { rooms = List.filter((fun r -> r != room), model.rooms) } 
  end
in
