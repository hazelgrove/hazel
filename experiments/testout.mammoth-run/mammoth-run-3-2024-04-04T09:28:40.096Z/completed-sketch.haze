# Update Password Strength Checker app model based on an action #

let update: (Model, Action) -> Model =
  fun (model, action) ->
  case action
    | AddTodo => ?? 
    | RemoveTodo(index) => ?? 
    | ToggleTodo(index) => ?? 
    | UpdateBuffer(str) => ?? 
  end
in