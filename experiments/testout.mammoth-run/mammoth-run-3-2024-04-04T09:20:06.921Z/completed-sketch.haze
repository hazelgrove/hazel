# Update the EmojiPaint app model based on an action #

let update: (Model, Action) -> Model =
  fun (model, action) ->
  case action
    | SetColor(color) => { model with color = color }
    | Draw(x, y) => { model with canvas = draw_on_canvas(model.canvas, model.color, x, y) }
  end
in
