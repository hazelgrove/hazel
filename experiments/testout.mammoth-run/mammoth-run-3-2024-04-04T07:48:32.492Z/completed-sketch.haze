# TODO MVU: Handle TODO actions to update the app model #

let update: (Model, Action) -> Model =
  
fun m, a ->
  case a
  | AddTodo => { m with todos = add(m) }
  | ToggleTodo(id) => { m with todos = toggle(id, m.todos) }
  | RemoveTodo(id) => { m with todos = remove(id, m.todos) }
  end
in
