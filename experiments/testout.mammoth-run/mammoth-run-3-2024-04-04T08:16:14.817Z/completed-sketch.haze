# Update Playlist app model based on an action #

let update: (PlayList, PlayListAction) -> PlayList =
  fun model, action ->
  case action
  | Add(song) => model.add(song)
  | Remove(song) => model.remove(song)
  | Clear => model.clear()
  | Shuffle => model.shuffle()
  end
in