fun model, action ->
case action
| AddBooking(user, weekday, time) => if bookingExists(model, (time, user, bookingId(model))) then model else addBooking(model, (time, user, bookingId(model)))
| RemoveBooking(id) => removeBooking(model, id)
| UpdateBuffer(text) => updateBuffer(model, text)
end