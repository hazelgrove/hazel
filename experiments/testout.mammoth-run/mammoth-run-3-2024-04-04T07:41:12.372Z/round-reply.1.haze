fun (buffer, todos), action ->
  case action
  | AddTodo => (buffer, todos)
  | RemoveTodo(i) => (buffer, List.removeAt(i, todos))
  | ToggleTodo(i) => 
      let (content, done) = List.nth(i, todos) in
      (buffer, List.update(i, (content, !done), todos))
  | UpdateBuffer(s) => (s, todos)
  end