# TODO MVU: Handle TODO actions to update the app model #

let update: (Model, Action) -> Model =
  fun model, action ->
  case action
  | ToDoAction.Increment => { model with count = model.count + 1 }
  | ToDoAction.Decrement => { model with count = model.count - 1 }
  | _ => model
  end
in
