# TODO MVU: Handle TODO actions to update the app model #

let update: (Model, Action) -> Model =
  fun (model, action) ->
  case action
  | SetText(text) => {model with text = text}
  | SetCount(count) => {model with count = count}
  end
in
