
fun model, action ->
  case action
  | SubmitPassword(new_password) => {model with password = new_password; strength = calculateStrength(new_password, model.criteria)}
  | UpdateCriteria(new_criteria) => {model with criteria = new_criteria; strength = calculateStrength(model.password, new_criteria)}
  | Reset => initialModel
  end