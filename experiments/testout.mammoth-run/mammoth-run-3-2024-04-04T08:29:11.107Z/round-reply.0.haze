
fun m, a ->
  case a of
  | AddBooking(user, weekday, timeOfDay) =>
    let form, bookings, id = m in
    let newBooking = ((weekday, timeOfDay), user, id + 1) in
    (initFormState, newBooking::bookings, id + 1)
  | CancelBooking(user, bookingId) =>
    let form, bookings, id = m in
    (form, rm_booking(user, bookingId, bookings), id)
  | ClearBookings =>
    (initFormState, [], 0)
  end