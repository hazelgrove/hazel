fun model, action ->
    case action
    | AddTodo => add(model)
    | RemoveTodo(idx) => remove(idx, model)
    | ToggleTodo(idx) => toggle(idx, model)
    | UpdateBuffer(str) => (str, snd(model))
    end