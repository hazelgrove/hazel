fun model, action ->
  case action
    | AddBooking(user, weekday, time) => if !bookingExists(model, (time, user, getBookingCount(model) + 1)) then model else addBooking(model, (time, user, getBookingCount(model) + 1))
    | RemoveBooking(id) => removeBooking(model, id)
    | UpdateBooking(id, time, user) => updateBooking(model, id, time, user)
  end