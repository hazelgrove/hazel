# Update the Room Booking app model based on an action #

let update: (Model, Action) -> Model =
  
fun model, action ->
  case action
  | BookRoom(roomId) => addBooking(model, roomId)
  | CancelBooking(bookingId) => removeBooking(model, bookingId)
  end
in
