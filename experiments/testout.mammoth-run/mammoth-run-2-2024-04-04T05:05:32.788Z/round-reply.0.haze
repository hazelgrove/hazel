fun model, action ->
  case action
  | AddTodo => add(model)
  | RemoveTodo(idx) => let (_, todos) = model in (String.empty, remove(idx, todos))
  | ToggleTodo(idx) => let (buffer, todos) = model in (buffer, toggle(idx, todos))
  | UpdateBuffer(str) => let (_, todos) = model in (str, todos)
  end