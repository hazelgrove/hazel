fun (model, action) ->
  case action
  | ChangePassword(new_password) => { model with password = new_password; strength = check_strength(new_password) }
  | ToggleVisibility => { model with visible = not model.visible }
  end