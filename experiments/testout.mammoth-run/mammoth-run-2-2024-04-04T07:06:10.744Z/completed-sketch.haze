# Update Password Strength Checker app model based on an action #

let update: (Model, Action) -> Model =
  
fun model, action ->
  case action
  | PasswordChanged(password) => { model with password = password; strength = calculate_strength(password) }
  | Submit => { model with submitted = true }
  end
in