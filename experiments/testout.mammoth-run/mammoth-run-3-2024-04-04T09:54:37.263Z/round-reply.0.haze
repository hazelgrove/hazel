fun model, action ->
  case action
  | ChangePassword(password) => {model with password = password; strength = calculate_strength(password)}
  | ChangeStrength(strength) => {model with strength = strength}
  | Reset => initial_model
  end
