
fun model, action ->
  case action
  | NoOp => model
  | Add => add(model)
  | Toggle(i) => {model with todos = toggle(i, model.todos)}
  | Remove(i) => {model with todos = remove(i, model.todos)}
  end