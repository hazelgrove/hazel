# Update Playlist app model based on an action #

let update: (PlayList, PlayListAction) -> PlayList =
  fun (playList, action) ->
    case action
    | PlaySong(id) => playList
    | PauseCurrentSong => playList
    | RemoveSong(id) => playList
    | AddSong(id) => playList
    end
in