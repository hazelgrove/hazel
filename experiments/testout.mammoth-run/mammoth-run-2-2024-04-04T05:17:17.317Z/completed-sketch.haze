# TODO MVU: Handle TODO actions to update the app model #

let update: (Model, Action) -> Model =
  fun model, action ->
  case action
  | AddTodo => Model.add(model)
  | RemoveTodo(i) => Model.remove(i, model.todos)
  | ToggleTodo(i) => Model.toggle(i, model.todos)
  end
in
