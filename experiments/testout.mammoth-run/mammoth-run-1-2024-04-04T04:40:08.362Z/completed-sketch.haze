# TODO MVU: Handle TODO actions to update the app model #

let update: (Model, Action) -> Model =
  fun model, action ->
  case action
  | Load => model
  | Save => model
  | Edit(text) => { model with text = text }
  | Delete => { model with text = "" }
  end
in
