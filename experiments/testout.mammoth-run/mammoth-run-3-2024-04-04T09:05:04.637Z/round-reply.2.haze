fun (model, action) ->
    case action
    | GridUpdated(row, col, emoji) => {model with grid = updateGrid(model.grid, row, col, emoji)}
    | RowFilled(row, emoji) => {model with grid = fillRowInGrid(model.grid, row, emoji)}
    | Clear => {model with grid = clearGrid(model.grid)}
    end