# Update Password Strength Checker app model based on an action #

let update: (Model, Action) -> Model =
  
fun model, action ->
  case action
  | ChangePassword(password) => { model | password = password }
  | ToggleShowPassword => { model | showPassword = not model.showPassword } 
  end
in