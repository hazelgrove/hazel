
fun model, action ->
  case action
  | SetRoom(room_id) => { model with room_id = room_id }
  | SetDate(date) => { model with date = date }
  | SetTime(time) => { model with time = time }
  | Submit => { model with submitted = true }
  end