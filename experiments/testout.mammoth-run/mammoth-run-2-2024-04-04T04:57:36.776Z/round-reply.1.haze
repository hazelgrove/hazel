fun (buffer, todos), action ->
  case action
  | AddTodo => if buffer != "" then ( "", (buffer, false)::todos) else (buffer, todos)
  | RemoveTodo(idx) => (buffer, List.filteri((fun i _ -> i != idx), todos))
  | ToggleTodo(idx) => (buffer, List.mapi((fun i (d, s) -> if i == idx then (d, !s) else (d, s)), todos))
  | UpdateBuffer(new_buffer) => (new_buffer, todos)
  end