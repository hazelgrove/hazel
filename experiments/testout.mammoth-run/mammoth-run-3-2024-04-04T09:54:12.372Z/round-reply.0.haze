fun model, action ->
    case action
    | PasswordChange(new_password) => {model with password = new_password; strength = calculate_strength(new_password)}
    | ToggleVisibility => {model with visible = !model.visible}
    end