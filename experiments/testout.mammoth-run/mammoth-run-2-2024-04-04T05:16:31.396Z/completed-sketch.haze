# TODO MVU: Handle TODO actions to update the app model #

let update: (Model, Action) -> Model =
  
fun m, a ->
  case a
  | Add(s) => add(m)
  | Toggle(i) => {m with todos = toggle(i, m.todos)}
  | Remove(i) => {m with todos = remove(i, m.todos)}
  end
in
