# Update Playlist app model based on an action #

let update: (PlayList, PlayListAction) -> PlayList =
  fun model, action ->
  case action
  | AddTrack(track) => Add(model.tracks, track)
  | DeleteTrack(track) => Delete(model.tracks, track)
  | Clear => Clear(model.tracks)
  end
in