# TODO MVU: Handle TODO actions to update the app model #

let update: (Model, Action) -> Model =
  
fun model, action ->
    case action
    | AddTodo() => let (buffer, todos) = model in (Model.init, add(model))
    | RemoveTodo(i) => let (buffer, todos) = model in (buffer, remove(i, todos))
    | ToggleTodo(i) => let (buffer, todos) = model in (buffer, toggle(i, todos))
    | UpdateBuffer(str) => let (buffer, todos) = model in (str, todos)
    end

in
