# TODO MVU: Handle TODO actions to update the app model #

let update: (Model, Action) -> Model =
  fun model, action ->
  case action
    | AddTodo => add(model)
    | RemoveTodo(idx) => {todos = remove(idx, model.todos)}
    | Toggle(idx) => {todos = toggle(idx, model.todos)}
  end
in
