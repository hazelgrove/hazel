fun model, action -> 
    case action
    | CheckPasswordStrength(password) => {
        model with 
          password_strength = measure_password_strength(password)
    }
    | ToggleShowPassword => {
        model with 
          show_password = not model.show_password
    }
    end
in update