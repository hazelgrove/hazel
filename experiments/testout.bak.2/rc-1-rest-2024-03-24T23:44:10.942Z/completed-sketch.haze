# TODO MVU: Handle TODO actions to update the app model #

let update: (Model, Action) -> Model =
  
fun model, action ->
  case action
  | Some(act) => update_action(act, model)
  | None => model
  end
where update_action: (Action, Model) -> Model  = fun action, model -> ?
in
