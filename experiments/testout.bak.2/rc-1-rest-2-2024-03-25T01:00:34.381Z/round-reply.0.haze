fun model, action ->
   case action 
   | CheckIn(reservation) => {...model, reservations = reservation::model.reservations }
   | CheckOut(reservation_id) => {...model, reservations = List.filter(fun r -> r.id != reservation_id, model.reservations)}
   | SetRoomCapacity(capacity) => {...model, roomCapacity = capacity} 
   end