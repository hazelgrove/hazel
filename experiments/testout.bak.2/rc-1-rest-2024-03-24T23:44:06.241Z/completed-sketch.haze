# TODO MVU: Handle TODO actions to update the app model #

let update: (Model, Action) -> Model =
  
fun model, action ->
  case action
  | NoOp => model
  | SomeAction(some_data) => {model with someField = some_data}
  end

in
