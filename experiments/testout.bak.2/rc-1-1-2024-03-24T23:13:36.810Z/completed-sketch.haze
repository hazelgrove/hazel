# Update Playlist app model based on an action #

let update: (PlayList, PlayListAction) -> PlayList =
  fun (model, action) ->
  case action
  | Add(song) => add_song_to_playlist(model, song)
  | Remove(song) => remove_song_from_playlist(model, song)
  | Clear => clear_playlist()
  end
in