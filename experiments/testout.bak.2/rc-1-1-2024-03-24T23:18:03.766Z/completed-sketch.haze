# Update Password Strength Checker app model based on an action #

let update: (Model, Action) -> Model =
  fun (model, action) ->
    case action
    | ChangePassword(pass) => {model with password = pass; passwordStrength = calcPasswordStrength(pass)}
    end
in