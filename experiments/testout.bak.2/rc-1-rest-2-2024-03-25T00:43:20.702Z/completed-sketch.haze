# Update Playlist app model based on an action #

let update: (PlayList, PlayListAction) -> PlayList =
  fun (playList, action) ->
case action
| FetchSongs => playList
| GetSongs => let songs = get_songs(playList) in playList
| GetState => let state = get_state(playList) in playList
end

in