# Update Playlist app model based on an action #

let update: (PlayList, PlayListAction) -> PlayList =
  fun model, action ->
  case action
  | AddTrack(trackId) => List.append(model, [trackId])
  | RemoveTrack(trackId) => List.delete(model, trackId)
  | ClearPlaylist => []
  end
in