# Update Playlist app model based on an action #

let update: (PlayList, PlayListAction) -> PlayList =
  
fun model, action -> 
  case action 
  | Add(song) => song::model
  | Remove(song) => List.filter(fun s -> s != song, model)
  | Clear => []
  end

in