fun model, action ->
  case action
  | UpdateForm(field, value) => { model with form = updateForm(model.form, field, value) }
  | SubmitForm => submitForm(model)
  | CancelBooking(bookingID) => cancelBooking(model, bookingID)
  end