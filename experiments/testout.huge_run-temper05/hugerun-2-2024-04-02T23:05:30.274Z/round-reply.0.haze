fun model, action ->
  case action
  | Toggle(idx) => {model with todos = toggle(idx, model.todos)}
  | Remove(idx) => {model with todos = remove(idx, model.todos)}
  | Add(text) => {model with todos = add({model with input = text})}
  | UpdateInput(text) => {model with input = text}
  end