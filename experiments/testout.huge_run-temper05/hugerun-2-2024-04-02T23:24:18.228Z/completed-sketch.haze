# Update Playlist app model based on an action #

let update: (PlayList, PlayListAction) -> PlayList =
  fun model, action ->
    case action
    | AddTrack(track) => model.add(track)
    | RemoveTrack(track) => model.remove(track)
    | Play => model.play()
    | Pause => model.pause() end
in