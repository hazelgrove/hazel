# Update Password Strength Checker app model based on an action #

let update: (Model, Action) -> Model =
  fun (model, action) ->
  case action
  | PasswordChanged(new_password) => { model with password = new_password }
  | ToggleShowPassword => { model with showPassword = !model.showPassword }
  end
in