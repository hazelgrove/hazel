fun model, action ->
  case action
  | AddTodo => add(model)
  | RemoveTodo(index) => remove(index, model)
  | ToggleTodo(index) => toggle(index, model)
  | UpdateBuffer(text) => update_buffer(text, model)
  end