# Update the EmojiPaint app model based on an action #

let update: (Model, Action) -> Model =
  fun model, action ->
  case action
  | AddEmoji(emoji) => Model.addEmoji(model, emoji)
  | RemoveEmoji(index) => Model.removeEmoji(model, index)
  | Clear => Model.clear(model)
  end
in
