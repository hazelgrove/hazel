# Update Password Strength Checker app model based on an action #

let update: (Model, Action) -> Model =
  fun (model, action) ->
  case action
  | SetPassword(p) => {model with password = p; strength = calculate_strength(p)}
  | SetStrength(s) => {model with strength = s}
  end
in