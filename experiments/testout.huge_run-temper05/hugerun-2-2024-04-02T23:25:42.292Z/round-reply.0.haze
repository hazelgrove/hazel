
fun model, action ->
    case action
    | AddTrack(track) => model.tracks::track
    | RemoveTrack(track) => List.filter(fun t -> t != track, model.tracks)
    | Play => { model with isPlaying = true }
    | Pause => { model with isPlaying = false }
    end