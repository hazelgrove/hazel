fun (model, action) ->
  case action
  | BookRoom(room) => {model with rooms = List.append(model.rooms, [room])}
  | CancelBooking(room) => {model with rooms = List.filter(fun r -> r != room, model.rooms)}
  | ChangeUser(user) => {model with user = user}
  end