fun model, action ->
    case action
    | AddTodo => add(model)
    | RemoveTodo(index) => remove(index, model)
    | ToggleTodo(index) => toggle(index, model)
    | UpdateBuffer(text) => (text, model)
    end