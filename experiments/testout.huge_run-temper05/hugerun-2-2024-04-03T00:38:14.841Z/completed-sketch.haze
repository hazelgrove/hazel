# Update Password Strength Checker app model based on an action #

let update: (Model, Action) -> Model =
  fun model, action ->
    case action
    | CheckPasswordStrength(password) => checkPasswordStrength(password)
    | _ => model
    end
in