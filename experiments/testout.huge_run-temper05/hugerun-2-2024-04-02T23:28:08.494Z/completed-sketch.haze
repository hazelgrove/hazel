# Update Playlist app model based on an action #

let update: (PlayList, PlayListAction) -> PlayList =
  fun model, action ->
    case action
    | Add(song) => List.append(model, [song])
    | Remove(song) => List.remove(model, song)
    | Clear => []
    end
in