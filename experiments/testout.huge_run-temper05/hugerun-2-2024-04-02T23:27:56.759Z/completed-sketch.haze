# Update Playlist app model based on an action #

let update: (PlayList, PlayListAction) -> PlayList =
  
fun model, action ->
  case action
  | AddTrack(track) => List.append(model, [track])
  | RemoveTrack(track) => List.remove(model, track)
  | ClearPlaylist => []
  end
in