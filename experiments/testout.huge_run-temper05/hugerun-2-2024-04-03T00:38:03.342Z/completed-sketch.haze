# Update Password Strength Checker app model based on an action #

let update: (Model, Action) -> Model =
  
fun m, a ->
  case a
  | SetPassword(p) => {m with password = p; strength = checkStrength(p)}
  | SetVisibility(v) => {m with visibility = v}
  end
in