# Update Password Strength Checker app model based on an action #

let update: (Model, Action) -> Model =
  fun (m, a) ->
  case a 
  | ChangePassword(new_password) => {m with password = new_password; strength = calculate_strength(new_password)}
  | _ => m
  end
in