# TODO MVU: Handle TODO actions to update the app model #

let update: (Model, Action) -> Model =
  fun (model, action) ->
  case action
  | NoOp => model
  | AddTodo => add(model)
  | ToggleTodo(i) => {model with todos = toggle(i, model.todos)}
  | RemoveTodo(i) => {model with todos = remove(i, model.todos)}
  end
in
