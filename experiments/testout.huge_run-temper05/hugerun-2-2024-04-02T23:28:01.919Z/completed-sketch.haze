# Update Playlist app model based on an action #

let update: (PlayList, PlayListAction) -> PlayList =
  fun model, action ->
  case action
  | Add(song) => PlayList.add(song, model)
  | Remove(song) => PlayList.remove(song, model)
  | Clear => PlayList.clear(model)
  end
in