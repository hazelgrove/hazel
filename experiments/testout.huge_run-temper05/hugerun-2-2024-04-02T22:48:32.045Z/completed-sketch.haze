# TODO MVU: Handle TODO actions to update the app model #

let update: (Model, Action) -> Model =
  fun m, a ->
  case a
  | AddTodo => add(m)
  | RemoveTodo(i) => remove(i, m)
  | ToggleTodo(i) => toggle(i, m)
  | UpdateBuffer(s) => update_buffer(s, m)
  end
in
