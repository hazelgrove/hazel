fun (password, criteria, strength), action ->
  case action
  | UpdatePassword(new_password) => (new_password, criteria, check_strength(new_password, criteria))
  | ClearCriteria => (password, [], Weak)
  | AddCriterion(new_criterion) => (password, List.append(criteria, [new_criterion]), check_strength(password, List.append(criteria, [new_criterion])))
  | RemoveCriterion(removed_criterion) => 
      (password, List.filter(criteria, fun c -> c != removed_criterion), check_strength(password, List.filter(criteria, fun c -> c != removed_criterion)))
  end
where check_strength: (Password, Criteria) -> Strength is fun password, criteria -> ?? end -- You would need to implement your own logic for `check_strength` function